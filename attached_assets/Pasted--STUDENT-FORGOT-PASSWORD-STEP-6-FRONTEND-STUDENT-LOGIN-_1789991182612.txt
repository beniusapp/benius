# STUDENT FORGOT PASSWORD — STEP 6
# FRONTEND / STUDENT LOGIN INTEGRATION
# USE THE COMPLETED BACKEND — DO NOT REDESIGN IT

Student Forgot Password backend is COMPLETE and PASS:

- Step 1 — Trusted recovery email = PASS
- Step 2 — Reset challenge persistence = PASS
- Step 3 — Server-bound recovery session = PASS
- Step 3B — Session-store CAS/race protection = PASS
- Step 4 — Forgot-password request + OTP verification = PASS
- Step 5 — Final password reset = PASS

Backend endpoints now exist:

POST /api/student/forgot-password
POST /api/student/verify-recovery-otp
POST /api/student/reset-password

The Student Login currently has NO Forgot Password link.

This step implements ONLY the user-facing Student Forgot Password flow.

==================================================
A. SCOPE
==================================================

Implement:

1. Student Login "Forgot Password?" link.
2. Student Forgot Password start screen.
3. OTP verification screen.
4. New Password screen.
5. Successful reset state.
6. Navigation between these states.
7. API integration with the already-existing backend.
8. Frontend validation.
9. Frontend tests.
10. Accessibility/basic UX validation.

DO NOT redesign Student Login.

DO NOT modify Student authentication logic.

DO NOT modify backend recovery security.

DO NOT modify Student Registry.

DO NOT modify Admin recovery.

DO NOT modify Teacher recovery.

==================================================
B. FIRST — READ-ONLY INSPECTION
==================================================

Before changing anything, inspect:

1. Current Student Login page/component.
2. Student routing configuration.
3. Student session provider.
4. Existing authentication API client conventions.
5. Existing form/input/button components.
6. Existing validation utilities.
7. Existing toast/alert/error components.
8. Existing loading states.
9. Existing responsive/mobile styles.
10. Existing Admin Forgot Password UI.
11. Existing Teacher Forgot Password UI.
12. Existing frontend test conventions.
13. Existing Student theme/layout.

Reuse existing components and visual conventions.

Do NOT create duplicate UI infrastructure.

==================================================
C. STUDENT LOGIN
==================================================

Add a clearly visible:

Forgot Password?

link/action to the existing Student Login page.

It must NOT interfere with:

- Digital Student ID input
- Password input
- Sign In button
- Register/activation link
- Existing validation
- Existing login behavior

Do not change the existing login API.

Do not change authentication logic.

The Forgot Password action should navigate to the Student recovery flow.

Use the application's existing routing convention.

Do NOT expose any recovery details on the Login page.

==================================================
D. RECOVERY FLOW
==================================================

The complete UI flow must be:

Student Login
    ↓
Forgot Password?
    ↓
Forgot Password screen
    ↓
OTP screen
    ↓
New Password screen
    ↓
Success
    ↓
Student Login

The browser must NOT receive or manage:

- challengeId
- resetToken
- password hash
- OTP
- Student internal ID
- school internal ID

==================================================
E. SCREEN 1 — FORGOT PASSWORD
==================================================

Create the Student Forgot Password screen.

It should collect:

1. School Code
2. Digital Student ID (DSID)

Use the same terminology and formatting conventions as Student Login.

Example:

School Code
[________________]

Digital Student ID
[________________]

[ Continue ]

Provide a way to return to:

Student Login

Do not collect recovery email from the Student.

Do not collect:

- studentId
- schoolId
- challengeId
- resetToken

The browser sends only:

schoolCode
+
dsid

to:

POST /api/student/forgot-password

==================================================
F. ANTI-ENUMERATION UI
==================================================

The UI must not reveal whether the submitted Student exists or whether a recovery email is configured.

Use a generic recovery response.

For example, the UI may say:

"If the account is eligible for password recovery, a verification code has been sent to the registered recovery email."

Do NOT display:

- Student email address
- partial email address
- Student name
- school database ID
- "email not registered"
- "Student not found"
- "wrong school"
- "inactive account"

Follow the backend's anti-enumeration behavior.

==================================================
G. OTP SCREEN
==================================================

After the forgot-password request is accepted, navigate to the OTP screen.

The screen should contain:

Verification Code

[ _ _ _ _ _ _ ]

[ Verify ]

Provide:

- resend/restart recovery option according to backend behavior
- return/cancel option
- loading state
- invalid-code state
- expired-code state

Do NOT implement client-side OTP validation that exposes backend state.

Basic format validation is fine:

- exactly 6 digits
- numeric characters only

The OTP is submitted to:

POST /api/student/verify-recovery-otp

Request body should contain ONLY the OTP unless the existing backend contract explicitly requires another non-authoritative field.

Do NOT send:

- studentId
- schoolId
- challengeId
- resetToken
- recovery email

==================================================
H. OTP SUCCESS
==================================================

When OTP verification succeeds:

navigate to:

New Password

The frontend must NOT receive a reset token.

Do not store a reset token in:

- React state
- localStorage
- sessionStorage
- cookies
- URL parameters
- browser history

The server-bound recovery session controls the next step.

==================================================
I. OTP FAILURE
==================================================

For invalid/expired/consumed recovery attempts:

show a safe generic message.

Do not expose:

- database errors
- challenge IDs
- session state
- reset-token information
- Student existence information

Preserve the OTP form when appropriate so the Student can retry.

Respect the backend's attempt/rate limits.

Do not implement a client-side retry counter that claims to know the authoritative remaining attempts.

==================================================
J. SCREEN 2 — NEW PASSWORD
==================================================

Create a New Password screen.

Fields:

New Password
[________________]

Confirm New Password
[________________]

[ Reset Password ]

Client-side validation:

- required
- minimum length according to the existing Student password policy
- confirmation must match

Do not duplicate complicated backend policy logic if it already exists.

The backend remains authoritative.

==================================================
K. PASSWORD SECURITY
==================================================

Never log the password.

Never place password in:

- URL
- query parameters
- localStorage
- sessionStorage
- analytics events
- console.log
- error logs

Send the new password only over the existing authenticated HTTPS/API mechanism to:

POST /api/student/reset-password

Request body:

{
  "newPassword": "..."
}

Do NOT send:

- studentId
- schoolId
- DSID
- challengeId
- resetToken
- recovery email

==================================================
L. PASSWORD RESET SUCCESS
==================================================

After successful reset show a clear success state.

Example:

"Password reset successful."

Then provide:

"Go to Student Login"

Do NOT automatically log the Student in.

Do NOT automatically create an authenticated Student session.

The backend already handles session revocation.

Return the Student to the normal Student Login screen.

==================================================
M. RESET FAILURE
==================================================

If the reset fails:

show a safe user-facing message.

Do not reveal:

- challenge details
- reset-token details
- database errors
- session internals
- Student internal identifiers

If the recovery session has expired or been consumed, provide a clear way to restart password recovery.

Do not pretend the password was changed when the API failed.

==================================================
N. BACK BUTTON / DIRECT NAVIGATION
==================================================

Handle recovery-step navigation safely.

Important:

The frontend must NOT assume that being on a particular screen means the server recovery state is valid.

The server remains authoritative.

If the Student:

- refreshes the page
- uses browser Back
- opens the recovery route directly
- returns after timeout

the UI must handle invalid/expired server state safely.

Do not place sensitive recovery data in URLs.

Do not encode:

- reset token
- challenge ID
- student ID
- school ID

in the route.

==================================================
O. REFRESH BEHAVIOR
==================================================

Do not persist recovery secrets in localStorage/sessionStorage.

The server-bound recovery session remains authoritative.

If a page refresh causes the client to lose its local UI step:

the next API request should determine whether the recovery session is still valid.

Do not invent a client-side recovery session.

==================================================
P. RESEND BEHAVIOR
==================================================

Inspect the existing backend behavior for repeated:

POST /api/student/forgot-password

Do not create a separate resend endpoint unless one already exists.

If the existing endpoint is used to restart recovery:

use the existing endpoint.

Respect:

- rate limits
- challenge replacement
- session replacement
- delivery-failure cleanup

Do not implement client-side bypasses.

Do not expose whether the resend actually found an account.

==================================================
Q. LOADING / DOUBLE SUBMISSION
==================================================

All recovery actions must prevent accidental duplicate submissions while a request is pending.

Disable the relevant button while awaiting:

- forgot-password request
- OTP verification
- password reset

Re-enable safely after failure.

Do not assume disabling a button provides server-side security.

Backend remains authoritative.

==================================================
R. ACCESSIBILITY
==================================================

Use existing application accessibility conventions.

Ensure:

- labels are associated with inputs
- keyboard navigation works
- focus moves logically between recovery steps
- errors are visible
- buttons have clear labels
- OTP input is accessible
- password fields use appropriate type/autocomplete attributes

For password fields use appropriate browser password-management semantics.

Do not expose recovery secrets through accessibility labels or debug text.

==================================================
S. RESPONSIVE DESIGN
==================================================

Use the existing Student Login responsive layout/theme.

The recovery screens must work on:

- desktop
- tablet
- mobile

Do not redesign the overall Student Portal.

Do not introduce a new visual design system.

==================================================
T. VISUAL CONSISTENCY
==================================================

The recovery screens should look like they belong to the existing Student Portal.

Reuse:

- typography
- colors
- buttons
- cards
- spacing
- form components
- alerts
- page layout

Do not redesign unrelated Student pages.

==================================================
U. ROUTING
==================================================

Use the existing Student routing architecture.

Possible route names may follow existing project conventions, for example:

/student/forgot-password
/student/forgot-password/otp
/student/forgot-password/reset

BUT:

inspect the actual routing structure first.

Do not blindly use these names if the application has a different convention.

Do not place secrets in route parameters.

==================================================
V. API ERROR HANDLING
==================================================

Inspect the actual response contracts of:

POST /api/student/forgot-password
POST /api/student/verify-recovery-otp
POST /api/student/reset-password

Do not invent response fields.

Handle:

- success
- validation failure
- rate limit
- expired recovery
- invalid OTP
- reset failure
- server error

using the application's existing frontend API conventions.

==================================================
W. NO BACKEND SECURITY CHANGES
==================================================

Do NOT change the already-passed backend security architecture merely to make the frontend easier.

Do not:

- add browser reset tokens
- add challenge IDs to URLs
- add client-side recovery authority
- weaken tenant checks
- change anti-enumeration behavior
- bypass server session checks
- create a second recovery mechanism

If the frontend discovers an API contract problem, STOP and report it rather than weakening the backend.

==================================================
X. TESTS
==================================================

Add frontend/component/integration tests covering:

1. Student Login displays Forgot Password.
2. Existing Student Login still works.
3. Forgot Password navigates to recovery.
4. Forgot Password screen accepts school code + DSID.
5. Only schoolCode + DSID are sent to forgot-password endpoint.
6. Generic anti-enumeration message is displayed.
7. Student email is never displayed.
8. OTP screen appears after successful request.
9. OTP input accepts exactly six digits.
10. OTP is sent to verify endpoint without challengeId.
11. Successful OTP moves to New Password.
12. Reset token is never present in frontend state.
13. Reset token is never placed in URL.
14. New Password screen appears after successful OTP.
15. Password confirmation validation works.
16. New password is sent only as newPassword.
17. Successful reset shows success state.
18. Student can return to Login.
19. Failed reset does not show false success.
20. Expired recovery can restart safely.
21. Refresh/direct navigation does not create client-side recovery authority.
22. Double submission is prevented at UI level.
23. Existing Student Login tests continue to pass.
24. Existing Student Portal tests continue to pass.

Do NOT test security by exposing actual secrets in test output.

==================================================
Y. SECURITY CHECK
==================================================

Perform a final frontend security search for:

- resetToken in localStorage
- resetToken in sessionStorage
- resetToken in URL
- challengeId in URL
- studentId in URL
- schoolId in URL
- console.log(password)
- console.log(otp)
- console.log(resetToken)
- console.log(newPassword)

No sensitive recovery values should be logged or persisted client-side.

==================================================
Z. READ-ONLY FIRST
==================================================

Before implementation inspect the actual code.

Do not assume filenames.

Identify:

- Student Login component
- Student route definitions
- Student layout
- API request helper
- form components
- existing Admin recovery UI
- existing Teacher recovery UI
- test conventions

Then implement the minimum required frontend changes.

==================================================
AA. SECURITY STOP RULE
==================================================

STOP and report instead of implementing if the frontend requires:

- reset token in browser storage
- reset token in URL
- challenge ID in URL
- browser-supplied Student identity
- browser-supplied school identity
- recovery email collection
- weakening backend anti-enumeration
- bypassing server-bound recovery session
- changing the already-tested backend security model

Do not implement an unsafe workaround.

==================================================
AB. VALIDATION
==================================================

Run:

1. Focused Student Forgot Password frontend tests.
2. Existing Student Login tests.
3. Existing Student Portal tests.
4. Student Step 1 recovery tests.
5. Student Step 2 recovery tests.
6. Student Step 3 recovery tests.
7. Student Step 3B recovery tests.
8. Student Step 4 recovery tests.
9. Student Step 5 recovery tests.
10. npm run check
11. git diff --check
12. Restart application.
13. Verify Student Login renders.
14. Verify Forgot Password flow renders.

No production deployment.

==================================================
AC. FINAL REPORT
==================================================

Return:

A. PASS / BLOCKED

B. Exact files changed

C. Student Login changes

D. Recovery routes/screens added

E. API integration

F. Frontend state management

G. Security review

H. Accessibility review

I. Responsive review

J. Tests:
   - suites
   - passed
   - failed
   - skipped

K. npm run check

L. git diff --check

M. Startup validation

N. Explicitly confirm:

- Forgot Password link added to Student Login
- Student Login authentication unchanged
- Student Registry unchanged
- Backend recovery endpoints unchanged
- No reset token in browser storage
- No reset token in URL
- No challenge ID in URL
- No Student internal ID in URL
- No school internal ID in URL
- No recovery email exposed
- No OTP logged
- No password logged
- No reset token logged
- No new client-side recovery authority
- No Admin changes
- No Teacher changes
- No schema/migration changes
- No production deployment

STATUS MUST BE:

PASS

or

BLOCKED