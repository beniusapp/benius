# MANUAL TEST — STUDENT FORGOT PASSWORD EMAIL DELIVERY DIAGNOSTIC
# READ-ONLY INVESTIGATION — DO NOT MODIFY CODE

I am manually testing the newly completed Student Forgot Password flow.

The Student Forgot Password UI is rendering correctly:

/student/forgot-password

The user enters:

- School Code
- Digital Student ID (DSID)

and clicks:

"Send recovery code"

However, the recovery code is NOT arriving at the student's verified recovery email.

IMPORTANT:
This is a diagnostic investigation only.

DO NOT modify:
- source code
- database schema
- migrations
- email configuration
- SendGrid configuration
- Student recovery logic
- password recovery logic
- frontend
- backend
- authentication
- session logic

Do NOT rotate credentials.

Do NOT send test emails to arbitrary addresses.

Do NOT expose or print:
- SendGrid API keys
- passwords
- OTP values
- reset tokens
- recovery email addresses
- Student IDs unnecessarily
- session cookies
- authorization headers

==================================================
1. TRACE THE ACTUAL REQUEST
==================================================

Inspect the existing implementation of:

POST /api/student/forgot-password

Determine exactly what happens after the request reaches the server.

Trace the execution path:

Student Forgot Password UI
→ frontend API call
→ /api/student/forgot-password
→ schoolCode resolution
→ DSID lookup
→ Student active/activated validation
→ verified recovery contact lookup
→ recovery challenge creation
→ school notification configuration lookup
→ email provider selection
→ SendGrid/Mailtrap delivery function
→ response to frontend

Do not guess.

Identify the exact source files and functions involved.

==================================================
2. CHECK THE ACTUAL RESPONSE
==================================================

Using the application's existing development environment, reproduce the request if safe.

Use a REAL test Student account that already has:

- active Student status
- activated account
- verified recovery email

Use the actual School Code and DSID only if they are already available in the development database.

DO NOT create or modify test data.

Capture only safe diagnostic information:

- HTTP status
- safe response body
- error category/message
- whether the challenge was created
- whether the email-send function was reached

DO NOT print OTP values.

DO NOT print recovery tokens.

DO NOT print API keys.

==================================================
3. CHECK SERVER LOGS
==================================================

Inspect the Replit server logs generated immediately after the manual request.

Determine whether the request:

A. never reaches the route

B. reaches route but fails Student lookup

C. reaches route but fails recovery-contact validation

D. creates the recovery challenge but email delivery is not attempted

E. reaches email delivery but notification configuration is missing

F. reaches SendGrid but SendGrid rejects the request

G. SendGrid accepts the request but delivery is delayed/bounced/rejected later

H. request succeeds but frontend receives an unexpected response

Report which exact stage is failing.

==================================================
4. CHECK STUDENT RECOVERY CONTACT
==================================================

Read-only inspect the relevant Student recovery records.

Verify:

- Student belongs to the expected school
- Student is active
- Student is activated
- recovery contact exists
- contact type is email
- contact belongs to the same Student
- contact belongs to the same school
- contact is verified
- contact matches the Student's current email as required by Step 4

DO NOT display the full email address in the report.

Use a masked representation if necessary, for example:

b***@gmail.com

DO NOT modify the record.

==================================================
5. CHECK SCHOOL NOTIFICATION CONFIGURATION
==================================================

Read-only inspect the existing notification configuration for the Student's school.

Verify:

- configuration exists
- configuration belongs to the same school
- emailEnabled is true
- provider is explicitly configured
- provider is supported by the Student recovery email implementation
- sender configuration exists
- required provider configuration is present

DO NOT reveal:

- API key
- secret
- credentials

Report only safe metadata, for example:

provider = sendgrid
emailEnabled = true
sender configured = yes

If credentials are missing, report:

"credential present: yes/no"

Never print the credential.

==================================================
6. TRACE PROVIDER SELECTION
==================================================

Inspect the existing email delivery implementation.

Confirm exactly how the Student Forgot Password flow selects its email provider.

Important:

There must NOT be an accidental fallback to:

- global SendGrid
- Admin SendGrid credentials
- Teacher SendGrid credentials
- another school's credentials
- environment-wide credentials

The Student recovery flow must use the authenticated/resolved school's notification configuration according to the already-approved architecture.

Do not change this behavior.

==================================================
7. CHECK SENDGRID CALL
==================================================

If the code reaches SendGrid:

Inspect the actual SendGrid request construction.

Verify:

- SendGrid client is initialized
- API key is read from the correct school notification configuration
- sender email is read from the correct school configuration
- recipient is the verified Student recovery email
- subject/template/content are generated
- send call is actually executed

DO NOT print the API key.

DO NOT print the complete email address.

DO NOT print the OTP.

Report only:

SendGrid call reached: YES/NO
SendGrid request constructed: YES/NO
SendGrid accepted request: YES/NO
Provider error category: <safe category if any>

==================================================
8. IMPORTANT — CHECK SENDGRID RESPONSE
==================================================

If the SendGrid API is actually called, capture only the safe result.

Determine whether SendGrid returns:

- success / accepted
- authentication failure
- forbidden
- invalid sender
- invalid recipient
- malformed request
- rate limit
- server error
- network error
- timeout
- other provider error

Do NOT expose the API key or complete recipient address.

If SendGrid returns a provider error, give the HTTP status and safe error category/message only.

==================================================
9. CHECK FOR DATABASE CHALLENGE CREATION
==================================================

After the test request, read-only verify whether a new:

student_password_reset_challenges

record was created.

Do not display:

- OTP hash
- reset token hash
- full Student identity
- sensitive values

Report only:

Challenge created: YES/NO

If YES, report:

- created timestamp
- expires approximately 10 minutes later
- attempts initially 0
- verifiedAt absent
- consumedAt absent

Do not modify or consume the challenge.

==================================================
10. CHECK DELIVERY VS APPLICATION SUCCESS
==================================================

Determine whether the application considers email delivery successful before SendGrid actually confirms acceptance.

Document the exact behavior.

For example:

Application:
challenge created → SendGrid accepted → HTTP 200

OR:

challenge created → SendGrid failed → challenge invalidated → generic response

OR:

challenge created → email function failed unexpectedly

Do not change behavior.

==================================================
11. CHECK SENDGRID DASHBOARD POSSIBILITY
==================================================

If application logs show that SendGrid accepted the message, state clearly:

"Application-side delivery request succeeded; actual mailbox delivery must be checked in SendGrid Activity/Email Activity."

Do NOT claim the email was delivered merely because the API returned success.

If the application never reached SendGrid, state that clearly.

==================================================
12. CHECK DEVELOPMENT ENVIRONMENT VARIABLES
==================================================

Inspect only the existence/configuration status of required environment variables.

Do NOT print values.

Report:

VARIABLE_NAME = PRESENT / MISSING

Only for variables actually used by the current Student recovery email implementation.

Do not assume variable names.

If the current architecture uses database-stored school credentials rather than environment credentials, explicitly state that.

==================================================
13. CHECK MAILBOX / SPAM
==================================================

If the SendGrid API accepted the message, do not immediately change application code.

The likely next diagnostic step would be checking:

- SendGrid Email Activity
- Spam/Junk
- Gmail filters
- sender authentication
- sender reputation
- bounce/block events

Clearly distinguish:

APPLICATION FAILURE

from

EMAIL PROVIDER / MAILBOX DELIVERY ISSUE.

==================================================
14. DO NOT FIX ANYTHING
==================================================

This is strictly a diagnostic step.

DO NOT:

- change email code
- change SendGrid credentials
- change notification config
- change Student recovery contact
- change database
- change migrations
- change routes
- change frontend
- change security logic
- add fallback providers
- add global email configuration
- weaken tenant isolation

If a configuration problem is discovered, report it.

If a code defect is discovered, report the exact defect and affected file/function.

Do not fix it yet.

==================================================
15. SECURITY REQUIREMENT
==================================================

The diagnostic output must NOT contain:

- SendGrid API key
- email password
- OTP
- OTP hash
- reset token
- reset token hash
- session cookie
- authorization header
- complete recovery email
- unrelated Student PII

Mask sensitive values where necessary.

==================================================
16. FINAL REPORT
==================================================

Return exactly:

A. STATUS

PASS — email pipeline reached provider successfully

or

BLOCKED — identify exact failing stage

B. REQUEST

Student Forgot Password route reached: YES/NO

HTTP status:
<status>

C. STUDENT RECOVERY ELIGIBILITY

Student active: YES/NO
Student activated: YES/NO
Verified recovery email exists: YES/NO
Recovery contact belongs to correct school: YES/NO
Recovery contact matches current Student email: YES/NO

Do not expose the actual email.

D. CHALLENGE

Challenge created: YES/NO

E. SCHOOL EMAIL CONFIG

Configuration exists: YES/NO
Correct school: YES/NO
Email enabled: YES/NO
Provider: <provider>
Sender configured: YES/NO
Credential present: YES/NO

Never expose credential values.

F. EMAIL DELIVERY

Email function reached: YES/NO
Provider request attempted: YES/NO
Provider accepted request: YES/NO

G. PROVIDER RESULT

HTTP status if available:
<status>

Safe error category/message:
<message>

H. ENVIRONMENT

List only relevant environment variable names and PRESENT/MISSING status.

I. ROOT CAUSE

Give ONE precise root cause if identifiable.

If not identifiable, say:

"Root cause not yet identifiable; next diagnostic evidence required: ..."

J. FILES INSPECTED

List exact files/functions inspected.

K. CHANGES MADE

Must say:

"NONE — read-only diagnostic."

L. SECURITY

Confirm no secrets were exposed in the diagnostic output.

STOP HERE.
Do not implement any fix.