STOP BEFORE STEP 2.

We have discovered a possible discrepancy in STEP 1 and need a READ-ONLY investigation.

DO NOT MODIFY CODE.
DO NOT MODIFY DATABASE DATA.
DO NOT create enrollments.
DO NOT modify students.
DO NOT modify exam scores.
DO NOT modify academic sessions.
DO NOT modify teacher authorization.

QUESTION:

The intended behavior is:

If an ACTIVE student exists in the Student Registry with:

school_id = the authenticated teacher's school
class = selected class
section = selected section

then that student MUST appear in Teacher Examination, regardless of whether the student has an enrollment record for the current academic session.

For example:

Teacher selects:
Class = 6
Section = B

Then EVERY active Student Registry student belonging to:

School 1
Class 6
Section B

must appear.

We believe there may be a glitch because the verification reported that School 1 has 2 active students in Class 6/B, but the Teacher Examination screen is not showing the expected roster.

INVESTIGATE ONLY.

STEP 1 — DATABASE TRUTH

Query the actual Student Registry and report EVERY student matching:

school_id = 1
class = 6
section = B

Include:

student id
student name
digital student id
is_active
school_id
class
section

Also report inactive Class 6/B students separately.

Do NOT change anything.

STEP 2 — DIRECT STORAGE QUERY

Run the exact storage function currently used by the roster:

storage.getStudentsByClassSection(
  1,
  "6",
  "B"
)

(or the exact values/types used by the application).

Report exactly what this function returns.

Compare its result with the raw Student Registry records from STEP 1.

If the raw database contains active Class 6/B students but the storage function does not return them, identify exactly why.

STEP 3 — EXACT SQL / ORM CONDITIONS

Show the exact SQL/Drizzle/ORM query generated by:

getStudentsByClassSection()

Verify every condition.

We expect ONLY:

students.school_id = 1
students.class = 6
students.section = B
students.is_active = true

There must be NO:

enrollment JOIN
academic_session condition
teacher assignment condition inside the student query
subject condition
exam_type condition

Report if any additional condition exists.

STEP 4 — FRONTEND REQUEST

Inspect:

client/src/pages/teacher-modules/examination.tsx

When the teacher selects:

Class 6
Section B

report the EXACT HTTP request generated by the frontend.

Confirm:

schoolId
class
section
subject
academic session/view session

Also confirm whether the class and section values sent to the backend are exactly:

class = "6"
section = "B"

and not another representation such as:

"Class 6"
"Section B"
"6 "
"B "
or another internal ID.

STEP 5 — BACKEND REQUEST

Trace the actual request:

GET /api/examination/roster/:schoolId/:class/:section

for:

school = 1
class = 6
section = B

Report:

- authenticated teacher
- requested schoolId
- className
- sectionName
- subject
- resolved academic session
- authorization result
- storage function called
- number of students returned
- student IDs returned

Do not modify anything.

STEP 6 — FRONTEND DISPLAY

Determine whether the API actually returns the students but the React UI fails to display them.

Specifically inspect:

- React Query key
- query enabled condition
- query response parsing
- loading state
- error state
- students state/default value
- rendering/filtering logic

Determine which of these three situations is actually occurring:

A. Student Registry has no matching active students.

B. Student Registry has matching students, but backend/storage does not return them.

C. Backend returns the correct students, but frontend does not display them.

Do NOT guess. Prove which one it is.

STEP 7 — COMPARE CLASS 6/A AND 6/B

We know Class 6/A currently returns:

Student 4
Student 7

Now compare:

Class 6/A → expected API result
Class 6/B → expected API result

Report the exact Student Registry rows and exact API results for both.

STEP 8 — MULTI-TENANT CHECK

Confirm that the investigation remains tenant-safe.

Only inspect School 1 data for this test.

Do not broaden the query or expose another school's student information unnecessarily.

IMPORTANT:

This is an investigation only.

NO CODE CHANGES.
NO DATABASE CHANGES.

At the end, give a clear conclusion:

"ROOT CAUSE = ______"

and identify whether the problem is:

1. Student Registry data
2. Class/section value mismatch
3. Backend storage query
4. Backend authorization/session logic
5. Frontend request
6. Frontend rendering/state
7. Something else

Then STOP.

Do not implement a fix.