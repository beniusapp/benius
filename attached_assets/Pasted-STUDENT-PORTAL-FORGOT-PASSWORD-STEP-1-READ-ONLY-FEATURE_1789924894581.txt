STUDENT PORTAL FORGOT PASSWORD — STEP 1
READ-ONLY FEATURE AUDIT AND DESIGN INVESTIGATION

IMPORTANT:
We are focusing ONLY on Student Portal → Forgot Password.

Do NOT redesign or modify the existing Student Registry.

Do NOT modify the existing Student Activation flow.

Do NOT fix the previously identified Student Activation vulnerability in this step.

Do NOT change any code, database, schema, migration, UI, configuration, or production data.

This is a READ-ONLY investigation.

==================================================
OBJECTIVE
==================================================

I want to build a secure:

Student Login
→ Forgot Password
→ Recovery Verification
→ Set New Password
→ Login

flow.

Before implementation, determine exactly what infrastructure already exists and what is required.

The Student currently logs in using:

- Digital Student ID (DSID)
- Password

The Student Registry already contains Student identity/contact information.

For this step, treat the existing Student Registry as the source of Student identity data.

Do not redesign the registry.

==================================================
1. STUDENT LOGIN — CURRENT IMPLEMENTATION
==================================================

Inspect and document:

- Student Login frontend
- Student login API
- DSID lookup
- Student identity lookup
- schoolId/tenant resolution
- password verification
- session creation
- session storage
- session cookie
- logout if any
- existing session-revocation mechanism

Relevant areas likely include:

- client/src/pages/student-login.tsx
- server/routes.ts
- server/storage.ts
- server/index.ts
- shared/schema.ts

Do not change anything.

==================================================
2. CONFIRM CURRENT STUDENT FORGOT PASSWORD
==================================================

Search the entire repository for:

- forgot-password
- forgot password
- reset-password
- reset password
- verify-otp
- OTP
- password reset
- passwordReset
- recovery
- reset token
- recovery token
- student password

Determine whether any Student Forgot Password implementation already exists.

If none exists, state clearly:

"No active Student Forgot Password flow exists."

==================================================
3. STUDENT RECOVERY IDENTITY
==================================================

Determine exactly how a Student should be identified for password recovery.

Inspect available Student fields:

- schoolId
- digitalStudentId / DSID
- email
- phone
- studentId
- date of birth
- guardian information
- any other relevant field

Important:

Do NOT propose DSID + phone + DOB as a password-recovery factor.

Those are knowledge-based attributes and must not be treated as sufficient proof of account ownership.

The existing activation issue is separate and must remain untouched.

==================================================
4. SCHOOL / TENANT BOUNDARY
==================================================

This application is a TRUE MULTI-TENANT SaaS platform.

Student Forgot Password MUST be tenant-bound.

Determine the safest recovery request identity.

The preferred conceptual flow is:

School Code
+
Student DSID
+
trusted recovery contact
→ exact school
→ exact Student
→ exact school's notification configuration

Never:

Student email
→ search all schools
→ find a Student
→ use that school's credentials

Never trust browser-supplied:

- schoolId
- tenantId
- studentId
- userId
- challengeId
- resetToken

for authorization.

Document the exact database query that would resolve:

school + DSID → Student

Do not implement it yet.

==================================================
5. RECOVERY CONTACT
==================================================

Inspect the existing Student Registry and determine:

- Is email available?
- Is email mandatory for new Students?
- Can existing Students have null email?
- Is phone available?
- Is phone mandatory?
- Is email ownership verified?
- Is phone ownership verified?
- Is there a verification timestamp?
- Is there a trusted recovery-contact field?
- Is there any existing contact-verification mechanism?

IMPORTANT:

Do not assume that an administratively entered email proves that the Student controls that email.

Determine whether the existing application has any trustworthy way to establish email ownership.

If email is not currently verified, document this clearly.

Do NOT add a fake `emailVerified = true` field.

Do NOT modify the Student Registry.

==================================================
6. SCHOOL-MANAGED EMAIL INFRASTRUCTURE
==================================================

Inspect the existing school-managed notification system.

Determine exactly how Student Forgot Password could use:

- SendGrid
- school notification settings
- sender email
- sender name
- email templates
- delivery helper
- timeout handling
- Mailtrap development configuration

The required security model is:

validated school
→ exact Student in that school
→ that school's notification configuration
→ email sent only to that Student's recovery contact

Never use:

global SendGrid credentials
global default school
another school's credentials
email-based school discovery

Document the existing helper functions that can safely be reused.

Do not modify them.

==================================================
7. ADMIN / TEACHER RECOVERY INFRASTRUCTURE
==================================================

Inspect the already-secured Admin and Teacher recovery implementations.

Determine which components can be reused conceptually or technically.

Inspect:

- OTP generation
- HMAC hashing
- reset-token generation
- reset-token hashing
- OTP expiry
- OTP attempt limits
- challenge replacement
- challenge consumption
- recovery session
- anti-enumeration responses
- rate limiting
- session invalidation
- durable revocation markers
- race protection
- transactional password reset

Important:

Students are stored in `students`, not `users`.

Determine which existing components assume:

- users.id
- users.schoolId
- userId
- Teacher identity

and therefore cannot be reused unchanged.

Do not modify the existing Admin/Teacher recovery systems.

==================================================
8. STUDENT-SPECIFIC RECOVERY CHALLENGE
==================================================

Determine whether the existing database can represent a Student recovery challenge.

Inspect:

- password_reset_challenges
- users
- students

Determine whether a Student-compatible challenge requires:

- a new table
- nullable studentId
- a subject/type field
- another safe design

Do NOT create the schema yet.

Recommend the safest design.

The challenge must ultimately be bound to:

- schoolId
- studentId
- recovery purpose
- OTP hash
- expiry
- attempt count
- reset-token hash if applicable
- verified timestamp
- consumed timestamp
- request metadata if appropriate

Do not implement.

==================================================
9. STUDENT PASSWORD RESET
==================================================

Inspect the existing Student password storage.

Determine:

- passwordHash field
- bcrypt cost
- password validation rules
- existing Student change-password route
- existing Student password update storage method

Do NOT modify the Student password-change route in this step.

However, explicitly determine whether it has a race condition with a future Forgot Password reset.

Specifically investigate:

Student authenticated change-password
VS
Student Forgot Password reset

Document whether the existing change-password implementation uses:

- transaction
- row lock
- compare-and-swap
- current-hash verification
- session invalidation

If it is unsafe, report it.

Do NOT fix it now.

==================================================
10. SESSION SECURITY
==================================================

Inspect Student session behavior.

Determine whether Student sessions currently have:

- studentId
- schoolId
- authIssuedAt
- session revocation identity
- durable revocation marker
- strict session invalidation
- stale-session rejection
- session resurrection protection

Compare the Student session architecture with the already-secured Teacher session architecture.

Determine what must be added for a secure Student password reset.

Do NOT implement it yet.

==================================================
11. ACCOUNT ENUMERATION
==================================================

Design the expected Forgot Password response behavior.

A recovery request must NOT reveal whether:

- DSID exists
- Student exists
- Student belongs to the supplied school
- email exists
- email matches
- Student is active
- Student is activated
- OTP was sent
- email delivery succeeded

Document the exact generic-response strategy.

Do not implement it.

==================================================
12. OTP SECURITY
==================================================

If OTP is recommended, define:

- cryptographically secure generation
- six digits
- ten-minute expiry
- five-attempt maximum
- atomic attempt increments
- single-use
- challenge replacement
- replay prevention
- HMAC storage
- timing-safe comparison

Do not generate or send any real OTP in this step.

==================================================
13. RESET TOKEN SECURITY
==================================================

Define the required reset-token behavior.

Recommended:

- cryptographically random token
- stored only as HMAC/hash
- never stored plaintext
- never logged
- never exposed unnecessarily
- fifteen-minute expiry
- single-use
- bound to Student + school + recovery challenge
- server-side recovery session binding

Do not implement.

==================================================
14. RATE LIMITING
==================================================

Inspect existing rate limiting.

Determine what Student Forgot Password would require for:

- IP
- Student/account
- school
- OTP verification
- reset attempts

Determine whether current process-local IP-only limiting is sufficient.

Do not implement rate limiting yet.

==================================================
15. EMAIL ENUMERATION / DELIVERY
==================================================

Determine how to handle:

- missing email
- invalid Student
- inactive Student
- inactive school
- invalid school code
- notification configuration missing
- SendGrid failure
- Mailtrap/development mode

All externally visible responses should remain generic.

Do not reveal provider errors to the Student.

Do not send any real recovery email during this audit.

==================================================
16. MOBILE COMPATIBILITY
==================================================

The Student Portal will eventually have a mobile application.

Determine whether the proposed recovery architecture can support mobile.

Document whether recovery depends on:

- browser cookies
- express-session
- sessionStorage
- localStorage
- React state
- server-side recovery session
- browser-only APIs

Do not design the mobile application yet.

==================================================
17. REQUIRED USER FLOW RECOMMENDATION
==================================================

Based on the actual existing architecture, recommend a secure Student Forgot Password flow.

Prefer a flow conceptually similar to:

Student Login
→ Forgot Password
→ School Code + DSID + trusted recovery contact
→ generic response
→ OTP
→ Verify OTP
→ Set New Password
→ success
→ Login

But do NOT assume this is automatically safe.

If the current Student email cannot be trusted as a recovery factor, explicitly state what must happen before it can safely be used.

Do not modify the registry in this step.

==================================================
18. TEST PLAN
==================================================

Do not create tests yet.

Instead, provide the tests that will be required during implementation.

At minimum include:

- valid recovery
- unknown school
- unknown DSID
- wrong school
- cross-tenant attempt
- missing email
- mismatched email
- enumeration protection
- OTP expiry
- OTP wrong attempts
- OTP replay
- OTP replacement
- reset-token expiry
- reset-token replay
- reset-token injection
- concurrent recovery
- password reset atomicity
- old password rejection
- new password acceptance
- session invalidation
- stale session rejection
- session resurrection
- recovery vs authenticated password-change race
- unrelated Student session preservation
- cross-school session preservation

==================================================
19. SECURITY FINDINGS
==================================================

Classify ONLY the findings relevant to Student Forgot Password as:

CRITICAL
HIGH
MEDIUM
LOW
INFO

Do not expand this into a general Student Registry audit.

The previously identified Student Activation vulnerability is OUT OF SCOPE.

Mention it only as:

"Existing Student Activation security issue is parked and was not modified in this step."

Do not block Forgot Password design merely because activation is imperfect.

However, do not reuse the insecure activation factors as recovery proof.

==================================================
MANDATORY READ-ONLY RULE
==================================================

DO NOT:

- edit files
- create files
- create migrations
- modify schema
- modify routes
- modify UI
- modify Student Registry
- modify Student Activation
- modify Teacher recovery
- modify Admin recovery
- send real emails
- send SMS
- change configuration
- modify production data

This step is investigation and architecture design only.

==================================================
FINAL REPORT
==================================================

Return exactly:

A. Overall Status

PASS / BLOCKED

B. Current Student Forgot Password

C. Student Recovery Identity

D. Tenant Isolation

E. Trusted Recovery Contact

F. School-Managed Email Infrastructure

G. Reusable Admin/Teacher Recovery Components

H. Student Recovery Challenge Design

I. Student Password Reset Design

J. Student Session Security

K. OTP Security

L. Reset Token Security

M. Rate Limiting

N. Enumeration Protection

O. Mobile Compatibility

P. Recommended Student Forgot Password Flow

Q. Required Database/Schema Changes

R. Required API Endpoints

S. Required Frontend Screens

T. Required Security Tests

U. Security Findings Relevant ONLY to Forgot Password

For every finding provide:

- Severity
- File
- Function/route
- Exact issue
- Impact
- Whether production-reachable

V. Files Changed

EXPECTED:

None.

W. Database/Schema Changes

EXPECTED:

None.

X. Final Recommendation

Clearly state whether we can proceed to:

"Student Forgot Password — Step 2 Implementation"

or whether a specific prerequisite must be resolved first.

IMPORTANT:

Do not implement anything.

Do not modify Student Activation.

Do not modify Student Registry.

Do not modify Admin/Teacher recovery.

STOP after the report.