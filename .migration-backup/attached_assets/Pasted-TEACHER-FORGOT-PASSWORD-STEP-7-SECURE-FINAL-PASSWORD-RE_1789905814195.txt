TEACHER FORGOT PASSWORD — STEP 7
SECURE FINAL PASSWORD-RESET HTTP BOUNDARY ONLY

Steps 1–6 are completed and accepted.

IMPORTANT:
Implement ONLY Step 7.

This step replaces the remaining legacy:

POST /api/teacher/reset-password

with the secure server-side recovery-session flow.

The final flow must become:

Forgot Password
→ School Code + Email
→ OTP
→ server-side PASSWORD_RESET state
→ New Password
→ atomic password reset
→ invalidate active sessions
→ destroy Teacher recovery state
→ generic success
→ frontend can later return to Login

DO NOT modify the Teacher Forgot Password frontend yet.
DO NOT modify teacher-login.tsx.
DO NOT modify normal Teacher login.
DO NOT change database schema.
DO NOT create migrations.
DO NOT modify legacy database columns.
DO NOT change Step 2's atomic password-reset storage function unless absolutely necessary to connect it correctly.
DO NOT change Steps 3–6 semantics.
DO NOT send email/SMS.
DO NOT expose the reset token to the browser.

==================================================
CURRENT ACCEPTED COMPONENTS
==================================================

Step 2:

resetTeacherPasswordForChallenge(
  challengeId,
  userId,
  schoolId,
  resetTokenHash,
  passwordHash,
  now?
)

This is the authoritative atomic password-reset operation.

Step 4:

verifyTeacherPasswordRecoveryOtp(...)

This generates a fresh reset token after successful OTP verification.

Step 5:

getTeacherPasswordRecoverySession(req, expectedStage?, now?)

clearTeacherPasswordRecoverySession(req)

Teacher recovery state after successful OTP verification:

{
  flow: "teacher_password_recovery",
  stage: "password_reset",
  challengeId,
  userId,
  schoolId,
  teacherId,
  resetToken,
  createdAt,
  updatedAt
}

Step 6:

POST /api/teacher/forgot-password

and

POST /api/teacher/verify-otp

are already secured.

==================================================
STEP 7 GOAL
==================================================

Secure the final password-reset HTTP boundary.

The browser request must contain ONLY:

{
  newPassword: string,
  confirmPassword: string
}

The browser must NOT supply:

- schoolId
- tenantId
- userId
- teacherId
- challengeId
- resetToken
- email
- schoolCode

All recovery identity and the reset token must come exclusively from:

getTeacherPasswordRecoverySession(req, "password_reset")

==================================================
FIRST: INSPECT EXISTING CODE
==================================================

Before modifying anything, inspect:

- existing POST /api/teacher/reset-password
- server/teacher-routes.ts
- server/teacher-password-recovery-routes.ts
- server/teacher-password-recovery-session.ts
- server/storage.ts
- resetTeacherPasswordForChallenge()
- existing Admin password-reset HTTP route
- password validation utilities
- bcrypt usage
- session invalidation utilities
- Express session configuration
- existing authentication/session tests

Do not assume the legacy route's current behavior is safe.

Document what it currently accepts and how it currently resets passwords before replacing it.

==================================================
FINAL RESET REQUEST CONTRACT
==================================================

POST:

/api/teacher/reset-password

Expected body:

{
  "newPassword": "...",
  "confirmPassword": "..."
}

No other client field is authoritative.

If the browser sends:

{
  "teacherId": 999,
  "userId": 999,
  "schoolId": 999,
  "challengeId": 999,
  "resetToken": "attacker-token",
  "newPassword": "...",
  "confirmPassword": "..."
}

the server must ignore the injected recovery identity completely.

The server must continue using the existing server-side recovery session.

==================================================
RECOVERY SESSION REQUIREMENT
==================================================

At the beginning of the route:

const recovery =
  getTeacherPasswordRecoverySession(req, "password_reset")

If no valid recovery session exists:

- reject the request
- return a generic failure
- do not reveal whether it expired, was malformed, already consumed, or never existed

Do not accept identity from the request body as a fallback.

Do not query the database using browser-supplied teacherId/userId/schoolId.

Do not accept a resetToken from the browser.

==================================================
SESSION IDENTITY
==================================================

Use only:

recovery.challengeId
recovery.userId
recovery.schoolId
recovery.teacherId
recovery.resetToken

The route must never substitute:

req.body.userId
req.body.teacherId
req.body.schoolId
req.body.challengeId
req.body.resetToken

for those values.

==================================================
PASSWORD VALIDATION
==================================================

Inspect the existing application's password policy and reuse the authoritative validation utility/convention.

Do NOT invent a weaker password policy.

Validate:

- newPassword exists
- confirmPassword exists
- both are strings
- newPassword === confirmPassword
- password satisfies the existing Teacher/user password requirements

Do not return password contents in errors.

Do not log passwords.

Do not trim passwords unless the existing application explicitly does so.

Passwords must be treated as exact user input.

If validation fails:

- do not call resetTeacherPasswordForChallenge()
- do not consume the recovery session
- return a generic password-validation error appropriate to the existing API conventions

==================================================
BCRYPT
==================================================

Use the existing bcrypt password hashing implementation.

Do not use:

- plaintext password storage
- MD5
- SHA-1
- SHA-256 as a password hash
- custom password hashing

Generate a proper bcrypt hash using the project's existing cost/configuration.

Do not log:

- plaintext password
- password hash
- reset token

==================================================
RESET TOKEN HANDLING
==================================================

The reset token is available ONLY from:

recovery.resetToken

It must never come from the browser.

Before calling Step 2:

- HMAC the server-side resetToken using the same existing password-recovery HMAC mechanism.
- Do not persist the plaintext token.
- Do not log it.

Pass only the resulting reset-token hash to:

resetTeacherPasswordForChallenge(
  recovery.challengeId,
  recovery.userId,
  recovery.schoolId,
  resetTokenHash,
  passwordHash
)

Do not duplicate the challenge validation logic in the route.

Step 2 remains the authoritative atomic transaction.

==================================================
ATOMIC PASSWORD RESET
==================================================

Call:

resetTeacherPasswordForChallenge(...)

exactly once for a valid reset attempt.

The storage transaction remains responsible for:

- challenge locking/validation
- exact challenge identity
- exact user identity
- exact school identity
- Teacher role
- Teacher/user linkage
- active-account validation
- reset-token hash match
- verifiedAt requirement
- consumedAt requirement
- reset-token expiry
- password update
- teachers.mustChangePassword=false
- consuming the successful challenge
- consuming other active recovery challenges for the same user+school
- transaction commit/rollback

Do not recreate these checks in the route.

==================================================
FAILURE BEHAVIOR
==================================================

If resetTeacherPasswordForChallenge() returns false:

- do not reveal why
- do not reveal whether token was wrong
- do not reveal whether challenge expired
- do not reveal whether challenge was already consumed
- do not reveal tenant/user details
- return a generic reset-failed response

Do not automatically create a new recovery challenge.

Do not send another OTP.

Do not fall back to legacy reset fields.

==================================================
SUCCESS BEHAVIOR
==================================================

Only after:

resetTeacherPasswordForChallenge() === true

perform the final recovery cleanup.

Success response should be generic, for example:

{
  "success": true,
  "message": "Your password has been reset successfully. Please log in again."
}

Do NOT:

- automatically log the Teacher in
- return a session-authentication token
- return userId
- return teacherId
- return schoolId
- return challengeId
- return resetToken
- return password hash

==================================================
SESSION INVALIDATION
==================================================

After successful password reset, invalidate all active authenticated sessions belonging to the affected user.

Use the application's existing session invalidation mechanism.

The reset operation MUST NOT leave an old authenticated Teacher session active.

This is important:

If the Teacher was previously logged in on another browser/device, that authenticated session must be invalidated after the password reset.

Use:

recovery.userId

as the authoritative user identity.

Do not use browser-supplied IDs.

==================================================
SESSION INVALIDATION ERROR HANDLING
==================================================

Inspect the existing session invalidation implementation.

If it currently suppresses errors or performs best-effort invalidation, do not silently assume that means security success.

Determine whether the existing implementation can guarantee invalidation of all active sessions.

For Step 7:

A successful password reset must not be reported as fully successful if the application cannot establish the required session invalidation guarantee.

However, do NOT break the already-atomic password-reset transaction by attempting to put session-store operations inside the database transaction unless the existing architecture explicitly supports that.

If invalidation is external to the DB transaction:

- password reset remains committed atomically
- session invalidation must be attempted immediately
- if invalidation fails, do NOT claim complete security success
- return a safe generic response according to the existing application error conventions
- record an internal server-side error without exposing details

Do not log passwords or reset tokens.

If the project already has a reliable session-version/revocation mechanism, reuse it.

==================================================
RECOVERY SESSION CLEANUP
==================================================

After a successful password reset:

Call:

clearTeacherPasswordRecoverySession(req)

This must remove ONLY:

req.session.teacherPasswordRecovery

It must not destroy unrelated session state before the broader authenticated-session invalidation has been handled.

The recovery session must not remain reusable.

A second POST /api/teacher/reset-password using the same recovery session must fail.

==================================================
ORDER OF OPERATIONS
==================================================

Use this conceptual order:

1. Read PASSWORD_RESET recovery session.
2. Validate password + confirmation.
3. Hash new password.
4. HMAC the server-side reset token.
5. Call atomic Step 2 password-reset transaction.
6. If false → generic failure; do not report success.
7. Invalidate all authenticated sessions for recovery.userId.
8. Clear Teacher recovery state.
9. Return generic success.

If the application's established session-invalidation architecture requires a different safe order, document it before changing it.

Do NOT claim success before required session invalidation has been handled.

==================================================
CONCURRENT RESET
==================================================

The Step 2 transaction already provides single-use/concurrency protection.

Do not add a second token-consumption mechanism in the route.

Test two concurrent valid reset requests using the same recovery session/token.

Expected:

- exactly one password reset succeeds
- the other must fail
- challenge cannot be replayed
- password state remains consistent

==================================================
OLD PASSWORD / NEW PASSWORD
==================================================

Test after successful reset:

- old password must fail authentication
- new password must authenticate successfully
- authenticated sessions created before reset are invalidated
- recovery session cannot reset again

Do not perform real production authentication changes.

Use isolated test fixtures.

==================================================
TENANT ISOLATION
==================================================

Create:

School A:
- Teacher A
- User A
- recovery challenge A
- reset session A

School B:
- Teacher B
- User B
- recovery challenge B

Attempt to inject School B values into the request while the server-side session is School A.

Examples:

{
  schoolId: schoolB,
  userId: userB,
  teacherId: teacherB,
  challengeId: challengeB,
  resetToken: tokenB,
  newPassword: "..."
}

Expected:

The request MUST continue using School A's server-side recovery state.

It must never reset User B.

Repeat with reverse direction.

==================================================
RESET TOKEN INJECTION TEST
==================================================

Create a valid PASSWORD_RESET session containing:

School A
User A
Teacher A
Challenge A
Token A

Send:

resetToken = Token B

in the browser body.

Expected:

Token B is ignored.

The route uses Token A from the server-side session.

Do not allow browser token replacement.

==================================================
IDENTITY INJECTION TESTS
==================================================

For each of these:

- schoolId
- tenantId
- userId
- teacherId
- challengeId

send an attacker-controlled value.

Confirm the final password update still targets ONLY the server-side recovery identity.

==================================================
RECOVERY SESSION REPLAY TEST
==================================================

After successful reset:

1. Repeat the same reset request.
2. Confirm it fails.
3. Confirm no second password update occurs.
4. Confirm no legacy reset field is consulted.

==================================================
PASSWORD VALIDATION TESTS
==================================================

At minimum:

- missing newPassword
- missing confirmPassword
- non-string values
- mismatched passwords
- password below minimum policy
- valid password
- Unicode password if supported by existing policy

Invalid password attempts must not consume the reset challenge.

==================================================
SESSION INVALIDATION TESTS
==================================================

Create multiple authenticated Teacher sessions for the same user:

- session A
- session B
- session C

Perform password reset through the recovery session.

Confirm all existing authenticated sessions are invalidated.

Then confirm:

- old password rejected
- new password accepted
- fresh login creates a new valid authenticated session

Do not confuse the anonymous recovery session with an authenticated Teacher session.

==================================================
RECOVERY SESSION CLEANUP TESTS
==================================================

After successful reset:

Assert:

req.session.teacherPasswordRecovery
is absent.

Then:

POST /api/teacher/reset-password

again

must fail.

==================================================
SECRET EXPOSURE TESTS
==================================================

Capture HTTP response and application logs.

Assert these never appear:

- plaintext password
- password hash
- resetToken
- resetTokenHash
- OTP
- SendGrid API key
- challenge secret

==================================================
LEGACY FIELD TESTS
==================================================

Confirm the new reset route never reads or writes:

- teachers.otpCode
- teachers.otpExpiresAt
- teachers.resetToken
- teachers.resetTokenExpiresAt

Those fields remain untouched.

==================================================
LEGACY RESET ROUTE REMOVAL
==================================================

The old insecure implementation must no longer be active.

There must be exactly one active:

POST /api/teacher/reset-password

implementation.

It must be the new secure server-session-based implementation.

Do not leave an alternate route registered under the same path.

==================================================
ADMIN REGRESSION
==================================================

Run all existing Admin Forgot Password/recovery tests.

Confirm:

- Admin recovery unchanged
- Admin session state unchanged
- Admin reset still works
- Teacher recovery namespace remains isolated

==================================================
NORMAL TEACHER LOGIN REGRESSION
==================================================

Run existing Teacher login tests.

Confirm:

- normal login unchanged
- password authentication unchanged except that the reset password becomes the new valid password
- no recovery state automatically authenticates the Teacher
- no automatic login after reset

==================================================
FRONTEND SCOPE
==================================================

DO NOT modify:

client/src/pages/teacher-login.tsx

Do not update frontend request payloads in Step 7.

The existing frontend may temporarily send an incompatible legacy payload.

That is intentional.

Frontend migration is a later step.

==================================================
DATABASE / SCHEMA SCOPE
==================================================

Do NOT:

- create migration
- alter schema
- create table
- add columns
- alter password-reset challenge schema
- remove legacy Teacher columns

Use the existing accepted schema and storage implementation.

==================================================
VALIDATION
==================================================

Run:

1. New Step 7 password-reset route tests.
2. Teacher recovery tests from Steps 1–6.
3. Admin recovery tests.
4. Password authentication tests.
5. Session invalidation tests.
6. Tenant-isolation/security tests.
7. npm run check
8. git diff --check
9. Restart application.
10. Confirm startup succeeds.

==================================================
STRICT SCOPE CHECK
==================================================

Before reporting completion, confirm Step 7 did NOT modify:

- teacher-login.tsx
- Teacher recovery frontend
- Forgot Password route behavior from Step 6
- OTP verification behavior from Step 4
- challenge creation behavior from Step 3
- recovery session structure from Step 5, except if a strictly necessary integration adjustment was required
- database schema
- migrations
- legacy Teacher OTP/reset columns
- normal Teacher login behavior
- SendGrid configuration
- SMS delivery

Only the final Teacher password-reset HTTP boundary and strictly necessary tests/integration code may change.

==================================================
FINAL REPORT
==================================================

Return:

A. Exact files changed
B. Exact final reset route
C. Final request body
D. Server-side recovery identity source
E. Server-side reset-token source
F. Password validation behavior
G. Password hashing behavior
H. Atomic reset invocation
I. Session invalidation mechanism
J. Recovery-session cleanup
K. Concurrent reset behavior
L. Tenant-isolation test results
M. Identity/token injection test results
N. Old/new password results
O. Existing-session invalidation results
P. Replay protection results
Q. Secret-exposure results
R. Legacy-field results
S. Admin regression results
T. Teacher-login regression results
U. Tests added and total results
V. npm run check
W. git diff --check
X. Application startup
Y. Database/schema/migration status
Z. Confirmation that frontend was NOT changed
AA. Any security/design issue discovered

FINAL STATUS:

PASS
or
BLOCKED

Do not proceed to Step 8 automatically.