TEACHER FORGOT PASSWORD — STEP 3
SECURE TEACHER RECOVERY CHALLENGE + OTP CREATION ONLY

Steps 1 and 2 are completed and accepted.

STEP 1:
Tenant-safe Teacher lookup was added:

getTeacherUserByEmailAndSchool(email, schoolId)

STEP 2:
Atomic Teacher password-reset transaction was added:

resetTeacherPasswordForChallenge(
  challengeId,
  userId,
  schoolId,
  resetTokenHash,
  passwordHash,
  now
)

Both steps passed their focused tests and existing Admin recovery regression tests.

IMPORTANT:
Implement ONLY Step 3 in this prompt.

DO NOT modify the Teacher Forgot Password HTTP routes.
DO NOT modify teacher-login.tsx.
DO NOT implement OTP verification.
DO NOT implement password reset.
DO NOT wire SendGrid/email delivery.
DO NOT implement session invalidation.
DO NOT modify Teacher login behavior.
DO NOT remove legacy Teacher OTP/reset fields.
DO NOT create a database migration.
DO NOT change the database schema.
DO NOT send a real email or SMS.

==================================================
GOAL
==================================================

Build the secure server-side Teacher recovery challenge creation layer.

This layer will later be called by the Teacher Forgot Password HTTP route.

It must:

1. Generate a cryptographically secure 6-digit OTP.
2. Generate a cryptographically secure reset token.
3. Store ONLY HMAC hashes of the OTP/reset token.
4. Create a tenant-bound password_reset_challenges record.
5. Enforce a 10-minute OTP expiry.
6. Set the challenge as unverified and unconsumed.
7. Replace/consume any previous active recovery challenge for the same Teacher + school.
8. Never log or return the plaintext OTP/reset token from production logging.
9. Never use Math.random().
10. Never use legacy Teacher recovery fields.

The HTTP route and email delivery will be implemented later.

==================================================
FIRST: INSPECT EXISTING SECURE ADMIN FLOW
==================================================

Before changing code, inspect the existing Admin password recovery implementation and shared helpers, especially:

- server/password-recovery.ts
- server/routes.ts
- server/storage.ts
- password_reset_challenges schema
- existing password recovery tests

Identify and reuse the existing secure helpers for:

- OTP generation
- HMAC hashing
- reset-token generation
- challenge creation
- challenge replacement/consumption
- expiry handling

Do NOT create duplicate cryptographic helpers if secure shared helpers already exist.

If the existing Admin helper already provides the exact secure behavior needed, reuse it.

==================================================
OTP REQUIREMENTS
==================================================

Teacher OTP must be:

- exactly 6 numeric digits
- generated using cryptographically secure randomness
- never generated with Math.random()
- never predictable
- never derived from user ID, school ID, timestamp, email, phone, or other deterministic values

Use the existing secure OTP generation helper if available.

The plaintext OTP must exist only in transient server memory long enough for the future email-delivery layer to use it.

DO NOT:

- store plaintext OTP in the database
- store plaintext OTP in the Teacher table
- store plaintext OTP in logs
- return plaintext OTP from an HTTP response
- return plaintext OTP from a future-facing storage API unless the existing secure Admin architecture explicitly requires a transient value

If the existing Admin implementation returns a transient OTP to its caller for delivery, follow that established secure pattern, but ensure it is never logged or persisted.

==================================================
OTP HASHING
==================================================

The database must store only the secure HMAC representation.

Use the existing application HMAC secret/configuration and helper.

Do NOT:

- invent a new secret
- hard-code a secret
- use a plain SHA-256 hash if the existing recovery system uses HMAC
- store the OTP itself
- store a reversible encryption of the OTP

The stored value must be compatible with the existing password_reset_challenges verification logic.

==================================================
RESET TOKEN
==================================================

Generate a cryptographically secure reset token for the later password-reset stage.

Requirements:

- cryptographically secure random bytes/token
- sufficient entropy
- never Math.random()
- never deterministic
- store ONLY its HMAC hash
- never store plaintext reset token in password_reset_challenges
- reset token expiry = 15 minutes
- token must be associated with the same challenge/user/school

Follow the existing Admin implementation exactly where possible.

IMPORTANT:

The reset token does NOT need to be sent to the browser in Step 3.

Do not modify any frontend behavior.

==================================================
CHALLENGE IDENTITY
==================================================

The created password_reset_challenges row MUST be explicitly bound to:

- userId
- schoolId
- teacher account represented by that user

Before creating the challenge, verify the Teacher relationship using the secure tenant-scoped lookup created in Step 1.

The challenge must never be created from:

- email alone
- teacher ID alone
- school code alone
- client-provided schoolId
- client-provided teacherId

The future route will resolve:

School Code
→ school
→ authenticated/request context schoolId
→ getTeacherUserByEmailAndSchool()
→ Teacher + User
→ challenge creation

Do not implement that route in this step.

==================================================
TENANT ISOLATION
==================================================

The challenge creation layer must require the trusted schoolId supplied by the future server-side flow.

Enforce:

user.schoolId = schoolId

AND

user.role = teacher

AND

user.isActive = true

AND

teacher.userId = user.id

AND

teacher.schoolId = schoolId

A Teacher belonging to School A must never receive/create a recovery challenge associated with School B.

Do not perform any global teacher lookup.

Do not use:

findTeacherByEmailAndPhone()

Do not use any legacy cross-tenant lookup.

==================================================
ACTIVE ACCOUNT REQUIREMENT
==================================================

Only an active Teacher may receive a new recovery challenge.

Reject:

- inactive user
- non-teacher user
- missing Teacher relationship
- Teacher/user school mismatch

Do not create a challenge in any of these cases.

==================================================
CHALLENGE EXPIRY
==================================================

OTP expiry:

10 minutes from challenge creation.

Use the application's existing time/date conventions.

Do not use browser-local time.

Use absolute timestamps/Date values consistent with the existing password recovery implementation.

Reset-token expiry:

15 minutes from challenge creation.

Do not shorten or extend these values.

==================================================
CHALLENGE REPLACEMENT
==================================================

When creating a new Teacher recovery challenge:

Any previous active challenge for the SAME:

- userId
- schoolId

must be invalidated/replaced according to the existing Admin recovery pattern.

Do NOT affect:

- another user in the same school
- the same user in another school
- another school entirely

The replacement operation must be tenant-bound.

The old challenge must no longer be usable for OTP verification.

==================================================
ATOMICITY
==================================================

Challenge replacement + new challenge creation should be atomic.

Conceptually:

BEGIN

1. Validate Teacher/user/school relationship.
2. Invalidate previous active challenge(s) for exact user + school.
3. Generate secure OTP.
4. Generate secure reset token.
5. Store HMAC hashes only.
6. Create new challenge with:
   - userId
   - schoolId
   - otpHash
   - otpExpiresAt = now + 10 minutes
   - verifiedAt = NULL
   - resetTokenHash
   - resetTokenExpiresAt = now + 15 minutes
   - consumedAt = NULL
   - attempts = 0
7. COMMIT

If creation fails:

ROLLBACK

The previous active challenge must not be left partially invalidated.

Use the existing database transaction conventions.

==================================================
OTP ATTEMPT STATE
==================================================

Initialize the new challenge's OTP attempt counter exactly according to the existing Admin recovery schema/pattern.

Do not implement OTP verification or lockout in this step.

However, the challenge must be created in a state compatible with the later secure OTP verification flow.

If the schema uses:

attempts = 0

initialize it accordingly.

If the existing Admin flow uses another field/default, preserve that convention.

==================================================
NO SECRET EXPOSURE
==================================================

During implementation and testing:

DO NOT print:

- OTP
- reset token
- OTP hash
- reset-token hash
- SendGrid API key
- password
- password hash

into application logs.

Do not add debug logging.

Test assertions may compare secure hashes where necessary, but do not print secrets.

==================================================
LEGACY TEACHER RECOVERY FIELDS
==================================================

DO NOT read or write:

- teachers.otpCode
- teachers.otpExpiresAt
- teachers.resetToken
- teachers.resetTokenExpiresAt

Do not remove them.

Do not clear them.

Do not migrate them.

The modern recovery system must use:

password_reset_challenges

only.

==================================================
NO EMAIL/SMS IN THIS STEP
==================================================

Do not send:

- SendGrid email
- SMS
- WhatsApp
- any external notification

The function should prepare the transient information that the future notification layer will need, following the existing Admin architecture.

No real provider credentials should be used.

==================================================
TESTS
==================================================

Add focused tests for Teacher challenge creation.

At minimum test:

1. Active Teacher + correct school creates challenge successfully.

2. Created challenge has correct userId.

3. Created challenge has correct schoolId.

4. Created challenge is explicitly tenant-bound.

5. OTP is exactly 6 numeric digits if returned to the transient caller.

6. OTP is cryptographically generated using the existing secure helper.

7. OTP hash stored in DB is NOT the plaintext OTP.

8. Reset-token hash stored in DB is NOT the plaintext reset token.

9. OTP expiry is exactly 10 minutes from supplied `now`.

10. Reset-token expiry is exactly 15 minutes from supplied `now`.

11. `verifiedAt` starts NULL.

12. `consumedAt` starts NULL.

13. OTP attempts initialize correctly.

14. Inactive Teacher cannot create a challenge.

15. Non-teacher user cannot create a challenge.

16. Missing Teacher relationship cannot create a challenge.

17. User/Teacher school mismatch cannot create a challenge.

18. Cross-tenant Teacher lookup cannot create a challenge.

19. Previous active challenge for same user + school is invalidated/replaced.

20. Previous challenge from another school remains unchanged.

21. Challenge for another user in same school remains unchanged.

22. Two separate challenge creations produce different secure OTP/reset-token values.

23. Legacy Teacher OTP/reset fields remain untouched.

24. Failed challenge creation does not partially invalidate the previous challenge.

25. No plaintext OTP/reset token is written to the database.

26. No plaintext OTP/reset token is emitted through application logging.

==================================================
SECURITY TEST
==================================================

Explicitly create:

School A:
- active Teacher A
- User A

School B:
- active Teacher B
- User B

Attempt to create a challenge using:

Teacher A/User A + School B

It MUST fail.

Then verify:

- no School B challenge was created for Teacher A
- existing School A recovery state is unchanged
- School B Teacher B remains unaffected

Also test the reverse direction.

==================================================
REPLACEMENT TEST
==================================================

Create:

Challenge 1
→ same Teacher + same school

Then create:

Challenge 2
→ same Teacher + same school

Verify:

- Challenge 1 cannot remain active
- Challenge 2 is the active challenge
- Challenge 1 cannot be used by the future OTP verification layer
- Challenge 2 contains a different OTP hash/reset-token hash
- Another school's challenge remains active

==================================================
FAILURE/ROLLBACK TEST
==================================================

Force the new challenge creation to fail after the old challenge would otherwise be invalidated.

Verify that the transaction rolls back:

- old challenge remains valid
- no partially created new challenge remains

Use the project's actual database/test mechanism.

Do not fake this test by merely skipping the insert.

==================================================
ADMIN REGRESSION
==================================================

Run the existing Admin password recovery tests.

Do not modify Admin behavior.

If shared helpers are reused/refactored, all existing Admin tests must remain green.

==================================================
VALIDATION
==================================================

After implementation run:

1. New Teacher challenge-creation tests.
2. Existing password-recovery tests.
3. Existing Admin tenant-isolation tests.
4. Relevant authentication/security tests.
5. `npm run check`
6. `git diff --check`
7. Restart the application and confirm startup succeeds.

==================================================
STRICT SCOPE CHECK
==================================================

Before reporting completion, verify this step did NOT change:

- Teacher Forgot Password HTTP routes
- teacher-login.tsx
- Teacher OTP verification
- Teacher password reset route
- SendGrid/email delivery
- SMS delivery
- Teacher recovery frontend
- session invalidation
- Teacher login behavior
- legacy Teacher OTP/reset fields
- database schema
- database migrations

Only the secure Teacher recovery challenge/OTP creation layer and its tests should be changed.

==================================================
FINAL REPORT
==================================================

Return:

A. Exact files changed
B. Exact function(s) added/changed
C. OTP generation mechanism
D. OTP storage/hash behavior
E. Reset-token generation/hash behavior
F. Tenant binding
G. Challenge replacement behavior
H. Expiry behavior
I. Atomicity/rollback behavior
J. Legacy field behavior
K. Email/SMS behavior
L. Tests added and results
M. Existing Admin recovery regression results
N. npm run check result
O. git diff --check result
P. Application startup result
Q. Database/schema/migration status
R. Confirmation that HTTP/frontend/reset/session behavior was NOT changed
S. Any unexpected issue

FINAL STATUS:

PASS
or
BLOCKED

Do not proceed to Step 4 automatically.