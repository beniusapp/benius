Fix Receipt PDF Download in Ledger & Transactions

I have tested the latest implementation carefully.

Current situation

The Invoice PDF works correctly.

In Admin Portal → Fees & Payments → Ledger & Transactions:

- Due records show an Invoice action.
- Paid records show a Receipt action.
- Clicking/opening the Receipt works and displays the receipt.
- However, there is currently no way to directly download the Receipt as a PDF from the Ledger.

This is the remaining issue.

Important

Do NOT create a new receipt PDF generator.

The server-side receipt PDF functionality has already been implemented and verified:

"GET /api/admin/fees/payments/:id/receipt/pdf"

It returns a genuine PDF and uses the existing receipt PDF renderer.

The problem is now the Ledger frontend/action wiring, not the PDF generator.

Required change

For every Paid row in:

Fees & Payments → Ledger & Transactions

make the Receipt action provide access to the existing Receipt PDF.

The behavior should be:

Paid row → Receipt → Receipt PDF

The PDF must be generated using the existing receipt PDF endpoint and existing receipt PDF renderer.

Preferred UI behavior

Keep the existing Receipt action.

When the user clicks Receipt, the existing receipt page should continue to work exactly as it does now.

But also provide a clear Download PDF action for that receipt.

For example:

Receipt | Download PDF

or, if the existing receipt page already has a download button, make sure that button correctly calls:

"/api/admin/fees/payments/:id/receipt/pdf"

and downloads the PDF.

Do not replace the existing HTML receipt.

Filename

The downloaded file should use the receipt number, for example:

"Receipt-ON23.pdf"

or

"Receipt-OF44.pdf"

Never use the database ID as the filename.

Important data mapping

Make sure the correct payment/transaction ID is passed to:

"/api/admin/fees/payments/:id/receipt/pdf"

Do not accidentally pass:

- fee_record ID
- invoice ID
- receipt number
- student ID

The endpoint requires the correct payment record ID.

Existing behavior that must remain unchanged

Do NOT modify:

- Invoice PDF generation
- Existing Receipt HTML renderer
- Existing Receipt PDF renderer
- Ledger filtering
- Transaction filtering
- Payment processing
- Invoice generation
- Receipt numbering
- Payment numbering
- Cash denomination validation
- Tenant isolation/security

Only fix the missing Ledger → Receipt → PDF connection.

Test thoroughly

Test at least:

1. Cash paid transaction → Receipt PDF
2. Demand Draft paid transaction → Receipt PDF
3. Cheque paid transaction → Receipt PDF
4. UPI/QR paid transaction → Receipt PDF
5. Bank Transfer/NEFT/RTGS/IMPS paid transaction → Receipt PDF
6. Razorpay/Online paid transaction → Receipt PDF

For each test verify:

- Correct receipt number
- Correct invoice number
- Correct student
- Correct amount
- Correct payment method
- Correct payment date/time
- Correct payment-specific details
- PDF opens successfully
- PDF downloads successfully on Chrome Android
- Filename is based on the receipt number
- No HTML is downloaded with a ".pdf" extension
- No email/privacy information is exposed

Final acceptance criteria

I should be able to go to:

Admin Portal → Fees & Payments → Ledger & Transactions

and see paid records with a working Receipt action.

From there, I must be able to obtain the same professional Receipt PDF that is already generated by the existing receipt PDF endpoint.

The Invoice PDF is already working. Do not change it.

This task is only to fix the missing Receipt PDF download/access from the Ledger.