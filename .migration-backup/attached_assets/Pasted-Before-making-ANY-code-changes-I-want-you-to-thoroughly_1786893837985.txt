Before making ANY code changes, I want you to thoroughly inspect the current Fees & Payments implementation and answer the questions below based strictly on the existing code, database schema, API routes, and frontend flow.

DO NOT MODIFY ANYTHING YET.

I have noticed there is a "Record Offline Payment" button in Ledger & Transactions.

The current modal allows the admin to select a student, fee type, amount, status, due date, academic year, payment method, received date, etc.

My concern is:

1. What exactly is "Record Offline Payment" currently doing?

When the admin clicks Record Offline Payment, is it:

- Creating a completely new fee/payment record?
- Creating a payment against an existing invoice?
- Creating a new invoice and immediately marking it Paid?
- Creating a payment transaction without any invoice?
- Or something else?

Trace the complete frontend → API → database flow and explain exactly what happens.

2. Is an invoice required?

I want to understand whether the current system allows an offline payment to exist without an invoice.

For example:

Student A has:

- Tuition Fee: ₹2,000
- August 2027 invoice has NOT been generated yet.

Can the admin currently click Record Offline Payment and record ₹2,000 as an offline payment?

If yes, explain exactly what database records are created and how that payment is later connected to an invoice, if at all.

3. What should the correct workflow be?

Please evaluate this proposed workflow:

Fee Structure
↓
Generate Invoice
↓
Student has a specific invoice
↓
Invoice status = Due
↓
Admin receives cash/cheque/bank transfer
↓
Admin clicks Record Offline Payment
↓
Admin selects the EXISTING invoice
↓
Payment is recorded against that invoice
↓
Invoice becomes Paid
↓
Receipt is generated/linked

Is this architecture better and more consistent for this application?

4. Should "Record Offline Payment" be invoice-based?

Please determine whether the offline payment feature should require the admin to select an existing unpaid invoice rather than manually entering:

- Student
- Fee Type
- Amount
- Academic Year
- Due Date

I want to avoid creating a payment that is not tied to a specific invoice.

5. Multiple invoices

Consider this exact example:

Student A has:

- August 2027 Tuition Invoice — ₹2,000 — Due
- August 2027 Lab Fee Invoice — ₹200 — Due
- September 2027 Tuition Invoice — ₹2,000 — Due

If the parent gives the school ₹2,200 in cash, the admin should be able to record the offline payment against the specific invoices being paid.

Explain how the current system handles this.

Also explain whether the current architecture can accidentally apply a payment to the wrong fee/invoice.

6. Partial payments

Please inspect whether the current system supports partial offline payments.

Example:

Invoice = ₹2,000

Admin receives ₹1,000 offline.

Should the invoice become:

Partially Paid — ₹1,000 paid / ₹1,000 remaining

Then later another ₹1,000 can be recorded against the SAME invoice.

Does the current implementation support this correctly?

7. Duplicate protection

Inspect whether the current offline payment flow can accidentally:

- Create two payments for the same invoice
- Record payment twice
- Create a payment without an invoice
- Pay an already-paid invoice
- Record more than the invoice's outstanding amount

Explain the current protections.

8. Payment history / audit trail

I want every offline payment attempt/transaction to be properly recorded without overwriting previous records.

Explain how the current implementation stores:

- Payment record
- Invoice relationship
- Payment amount
- Payment method
- Received date
- Admin/user who recorded it
- Receipt
- Payment status
- Audit information

9. Important architectural question

Please answer this clearly:

In our current school fee architecture, is there actually any valid reason for an admin to record an offline payment WITHOUT first having an invoice?

If there is a legitimate use case, explain it.

If there isn't, explain why the current "Record Offline Payment" flow should be changed to an invoice-based payment flow.

10. Do NOT make changes yet

For now, ONLY provide an analysis/report.

Do not modify:

- Database schema
- API routes
- Frontend
- Invoice generation
- Payment processing
- Razorpay integration
- Webhooks
- Receipts
- Existing payment records

I want your response structured as:

A. Current implementation
B. What happens when Record Offline Payment is clicked
C. Database records created
D. Whether an invoice is currently required
E. Problems/risks with the current design
F. Recommended architecture
G. Exact proposed offline-payment workflow
H. How multiple invoices should work
I. How partial payments should work
J. Duplicate-payment protection
K. Whether Record Offline Payment should be changed
L. Files/routes/database tables involved

Most importantly, base every answer on the ACTUAL existing code. Do not give a generic answer or assume how the system works.

Again: ANALYZE ONLY. DO NOT IMPLEMENT ANYTHING.