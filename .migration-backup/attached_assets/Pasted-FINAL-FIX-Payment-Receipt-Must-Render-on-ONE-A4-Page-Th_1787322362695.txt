FINAL FIX: Payment Receipt Must Render on ONE A4 Page

The Payment Receipt is still rendering as 2 pages ("1/2") in Chrome's Save as PDF preview.

The one-page requirement has NOT been achieved.

Do not consider this task complete until the normal UPI, Online/Razorpay, Cash, and Demand Draft receipts render as exactly "1/1".

Objective

Make the existing Payment Receipt fit completely on ONE A4 portrait page when printed/saved as PDF.

This is a print-only layout optimization.

Do NOT redesign the receipt.

Keep the existing:

- Receipt design
- Colors
- Typography hierarchy
- Cards
- Borders
- Tables
- Header
- School logo
- Student information
- Payment summary
- Fee details
- Payment details
- Transaction details
- Notes
- Authorized signature
- Footer
- All existing data
- Existing payment-specific fields

Nothing should be removed, hidden, truncated, or replaced.

---

1. IMPORTANT: The current problem

The Chrome Save as PDF preview still shows:

"1/2"

for normal receipts.

The remaining overflow is small.

UPI / QR receipt

Page 1 contains almost everything, while page 2 contains:

- Notes
- Authorized Signature
- Footer

Online / Razorpay receipt

Page 1 contains almost everything, while page 2 contains:

- Transaction Verification
- Authorized Signature
- Footer

This means the receipt is very close to fitting on one page.

Do NOT rebuild the receipt.

We only need to recover enough vertical space to bring these final sections onto page 1.

---

2. Fix the print CSS only

Inspect the existing receipt renderer and its current "@media print" CSS.

Make the smallest targeted changes necessary.

Further reduce, only for print:

- Section "padding"
- Section "margin"
- Card gaps
- Row gaps
- Header spacing
- Footer spacing
- Line heights
- Table row heights
- Space between section heading and content
- Space around signature
- Space above/below notes
- Space between major sections

The goal is to save the small amount of vertical space required to bring the remaining content onto page 1.

Do NOT make the receipt excessively small.

It must remain:

- Professional
- Readable
- Clean
- Well aligned

---

3. IMPORTANT: Force A4 correctly

The Chrome preview currently shows:

Paper size: Letter

This must be investigated.

The receipt is supposed to use A4.

Ensure the print CSS explicitly contains:

@page {
  size: A4 portrait;
  margin: 6mm;
}

Also inspect the existing print styles for anything that may override this.

Check for:

- Another "@page" rule
- "size: Letter"
- Fixed page dimensions
- Fixed heights
- Container widths/heights
- Browser-specific print CSS
- CSS that causes an unnecessary page break

Do not assume that adding "@page" alone solved it.

Verify the actual Chrome print preview.

---

4. Do NOT change the normal screen version

The receipt displayed normally in the browser is already correct.

Do not change its screen layout.

Any compacting should happen under:

@media print {
   ...
}

The browser/web receipt should continue looking exactly as it does now.

---

5. Fix page-break behavior intelligently

Inspect all existing:

page-break-before
page-break-after
page-break-inside
break-before
break-after
break-inside

Remove or adjust any unnecessary page-break rules that are forcing content onto page 2.

Use:

break-inside: avoid;
page-break-inside: avoid;

only where appropriate.

Do NOT apply "break-inside: avoid" to a huge parent container if that causes the entire section to jump to page 2.

The objective is natural one-page flow.

---

6. Specifically fix the remaining overflow

UPI / QR

Bring these onto page 1:

Notes
Authorized Signature
Footer

Online / Razorpay

Bring these onto page 1:

Transaction Verification
Authorized Signature
Footer

Do not remove any of these sections.

Do not hide them.

Do not shorten their data.

Do not truncate transaction IDs.

---

7. Preserve all payment methods

The receipt must remain one page for normal receipts for:

Cash

Keep:

- Cash denomination details
- Amount
- Processed By
- Notes
- Signature
- Footer

UPI / QR

Keep:

- UPI Transaction ID
- Amount
- Processed By
- Notes
- Signature
- Footer

Online / Razorpay

Keep:

- Payment Gateway
- Payment ID
- Order ID
- Payment Mode
- Transaction Status
- Application Recorded
- Transaction Verification
- Payment ID
- Order ID
- Provider
- Signature Verified
- Captured At
- Recorded At
- Signature
- Footer

Demand Draft

Keep:

- DD Number
- DD Date
- Branch
- Amount
- Processed By
- Notes
- Signature
- Footer

Do not remove payment-specific information.

---

8. Do NOT introduce another PDF implementation

The current mechanism is already correct:

Click Receipt
        ↓
window.open(receipt URL + ?print=1)
        ↓
Receipt HTML opens
        ↓
window.print()
        ↓
Chrome Print / Save as PDF

Keep this architecture.

Do NOT add:

- PDFKit
- Puppeteer
- Playwright
- New PDF endpoints
- Separate PDF buttons
- Server-side PDF generation
- A second receipt renderer

The existing HTML receipt is the source of the PDF.

---

9. Verify actual Chrome Print Preview

This is extremely important.

Do not only inspect the HTML.

Test the actual workflow:

Ledger & Transactions
→ Click Receipt
→ Chrome opens receipt
→ Print dialog appears automatically
→ Save as PDF
→ Inspect PDF preview

The preview must show:

1/1

not:

1/2

---

10. Acceptance criteria

The task is NOT complete unless all of these are true:

- [ ] UPI receipt = "1/1"
- [ ] Online/Razorpay receipt = "1/1"
- [ ] Cash receipt = "1/1"
- [ ] Demand Draft receipt = "1/1"
- [ ] Print size is A4 portrait
- [ ] Chrome preview no longer defaults the receipt to Letter because of our CSS
- [ ] No data is removed
- [ ] No fields are hidden
- [ ] No transaction IDs are truncated
- [ ] No content is clipped
- [ ] Signature remains visible
- [ ] Footer remains visible
- [ ] Notes remain visible
- [ ] Transaction Verification remains visible for online payments
- [ ] Existing receipt design is preserved
- [ ] Normal browser/screen receipt is unchanged
- [ ] Existing "?print=1" behavior is unchanged
- [ ] Invoice printing is completely untouched
- [ ] No new PDF system is introduced

Final instruction

Do not tell me "done" while Chrome still shows "1/2".

Actually verify the Chrome Save as PDF preview for UPI, Online/Razorpay, Cash, and Demand Draft.

The final expected result is:

Receipt → Print → A4 → complete receipt → "1/1"

Make only the minimum print-CSS/layout changes necessary to achieve this.