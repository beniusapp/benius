TEACHER FORGOT PASSWORD — STEP 8
FRONTEND INTEGRATION ONLY

Steps 1–7 are completed and accepted.

The secure Teacher Forgot Password backend is now complete.

Implement ONLY Step 8: update the Teacher Login frontend so it correctly uses the new secure backend flow.

==================================================
SECURE BACKEND CONTRACT — DO NOT CHANGE
==================================================

The backend now provides:

POST /api/teacher/forgot-password

Request:

{
  "schoolCode": string,
  "email": string
}

Generic response:

{
  "message": "If those details match, an OTP has been sent to your recovery email. Please check and try again."
}

--------------------------------------------------

POST /api/teacher/verify-otp

Request:

{
  "otp": string
}

The server obtains:

- challengeId
- userId
- schoolId
- teacherId

from the server-side Express recovery session.

The browser must NOT send those values.

Successful verification changes the server-side recovery session to:

PASSWORD_RESET

The reset token remains server-side.

The browser does NOT receive the reset token.

--------------------------------------------------

POST /api/teacher/reset-password

Request:

{
  "newPassword": string,
  "confirmPassword": string
}

The server obtains the recovery identity and reset token exclusively from the server-side recovery session.

The browser must NOT send:

- schoolId
- tenantId
- userId
- teacherId
- challengeId
- resetToken
- email
- schoolCode

The final response tells the user to log in again.

==================================================
CRITICAL RULE
==================================================

DO NOT modify the secure backend implementation from Steps 1–7.

Do not modify:

- server/teacher-password-recovery-routes.ts
- server/teacher-password-recovery-session.ts
- server/password-recovery.ts
- server/teacher-password-recovery.ts
- server/storage.ts
- server/session-revocation.ts
- server/index.ts

unless a TypeScript/import error makes a strictly necessary compatibility correction unavoidable.

If any backend change appears necessary, STOP and report it instead of making unrelated backend changes.

==================================================
FIRST: INSPECT EXISTING FRONTEND
==================================================

Read:

client/src/pages/teacher-login.tsx

Also inspect only the frontend helpers/hooks/components necessary to understand:

- Teacher login API calls
- form state
- navigation
- toast/error handling
- responsive/mobile layout
- password validation conventions
- existing loading states

The previous Teacher Forgot Password UI may currently expect the insecure legacy API contract.

Do not preserve that insecure contract.

==================================================
TARGET USER FLOW
==================================================

The Teacher Login page must support:

NORMAL LOGIN

Teacher Login
→ successful authentication
→ existing Teacher portal behavior

FORGOT PASSWORD

Teacher Login
→ Forgot Password
→ School Code + Registered Teacher Email
→ Send OTP
→ OTP Verification
→ New Password + Confirm Password
→ Password Reset Successful
→ Back to Teacher Login
→ Teacher logs in using new password

Do NOT automatically log the Teacher in after password reset.

==================================================
FORGOT PASSWORD STAGE
==================================================

The first recovery screen must request:

1. School Code
2. Registered Teacher Email

Do NOT request:

- phone number
- teacherId
- userId
- schoolId
- tenantId

The frontend sends exactly:

{
  schoolCode,
  email
}

to:

POST /api/teacher/forgot-password

Do not send additional identity fields.

==================================================
GENERIC RESPONSE
==================================================

The frontend must not attempt to determine whether:

- school exists
- teacher exists
- email exists
- account is inactive
- tenant mismatch occurred

Always display the backend's generic recovery message according to existing UI conventions.

Do not create frontend-specific account-enumeration messages such as:

"Teacher not found"

"Invalid school"

"No account exists"

"Wrong school"

==================================================
OTP STAGE
==================================================

After a successful Forgot Password request:

Move the UI to OTP verification.

Show:

- OTP input
- Verify OTP button
- appropriate loading state
- generic error state
- option to return to login

OTP input requirements:

- exactly 6 digits
- numeric input
- no identity fields
- no reset-token field

Send exactly:

{
  otp
}

to:

POST /api/teacher/verify-otp

DO NOT send:

- schoolCode
- email
- schoolId
- tenantId
- teacherId
- userId
- challengeId
- resetToken

The server-side session is authoritative.

==================================================
OTP SUCCESS
==================================================

After successful OTP verification:

Move the UI to password-reset stage.

Do not expect a reset token in the response.

Do not store any reset token in:

- React state
- localStorage
- sessionStorage
- URL
- query string
- cookie created by frontend code

The frontend should not know the reset token exists.

==================================================
PASSWORD RESET STAGE
==================================================

Display:

1. New Password
2. Confirm Password

Send exactly:

{
  newPassword,
  confirmPassword
}

to:

POST /api/teacher/reset-password

Do NOT send:

- teacherId
- userId
- schoolId
- tenantId
- challengeId
- resetToken
- email
- schoolCode

The server-side recovery session remains authoritative.

==================================================
PASSWORD VALIDATION
==================================================

Match the existing backend contract:

- both values must be strings
- minimum 6 characters
- values must exactly match
- do not trim passwords

Frontend validation is for user experience only.

The backend remains authoritative.

Do not weaken backend validation.

Do not transform the password before sending it.

Unicode passwords must remain supported.

==================================================
SUCCESS SCREEN
==================================================

After successful password reset:

Show a clear success state.

Example:

"Password reset successfully."

"Please log in with your new password."

Provide:

"Back to Login"

Do NOT automatically authenticate the Teacher.

Do NOT automatically redirect into the Teacher Portal.

Do NOT retain recovery state unnecessarily.

==================================================
BACK TO LOGIN
==================================================

When the user chooses Back to Login:

Return to the normal Teacher Login screen.

Clear frontend recovery state:

- school code
- email
- OTP
- new password
- confirm password
- recovery stage
- loading/error state

Do not clear or manipulate the server session directly from frontend code.

The backend recovery session is already consumed/cleared after successful reset.

==================================================
BROWSER SECURITY
==================================================

Do NOT store recovery secrets in browser storage.

Specifically, do not use:

localStorage
sessionStorage
IndexedDB

for:

- OTP
- resetToken
- challengeId
- userId
- teacherId
- schoolId

Do not put any recovery secret in URL parameters.

Do not put recovery identity into hidden form fields.

Do not expose server-side session contents.

The browser only needs to maintain the current UI stage.

==================================================
ERROR HANDLING
==================================================

Handle these backend responses gracefully:

- generic Forgot Password response
- invalid OTP
- expired OTP
- too many OTP attempts
- missing/expired recovery session
- password validation failure
- expired reset state
- reset failure
- session invalidation failure
- server error

Do not expose internal security details.

Do not display:

- challenge IDs
- user IDs
- teacher IDs
- school IDs
- database errors
- SendGrid errors
- reset tokens
- OTP values
- stack traces

Use the application's existing toast/error UI conventions.

==================================================
LOADING / DOUBLE-SUBMIT PROTECTION
==================================================

Each recovery action must have a loading state.

Prevent duplicate submissions while a request is in progress:

- Forgot Password
- Verify OTP
- Reset Password

Do not send multiple simultaneous requests because the user double-clicked.

The backend remains authoritative for concurrency/security.

==================================================
OTP INPUT UX
==================================================

Use a clean six-digit OTP input appropriate for the existing Teacher Login design.

Requirements:

- numeric input
- max length 6
- easy mobile input
- clear validation
- accessible label
- visible error state
- submit disabled while loading

Do not create unnecessary multi-input complexity if the existing UI design favors one input.

==================================================
PASSWORD UX
==================================================

Provide appropriate password fields.

If the existing application has a show/hide password convention, reuse it.

Do not introduce a different password policy display unless needed.

Never display the password value in logs or debug output.

==================================================
SCHOOL CODE UX
==================================================

School Code should be clearly labeled.

The frontend may normalize the value for user convenience if that matches existing application conventions, but the backend remains authoritative.

Do not derive or guess school identity from email.

Do not store school-specific SendGrid information in frontend code.

==================================================
SESSION / COOKIE BEHAVIOR
==================================================

Use the existing fetch/API mechanism so the browser sends the normal session cookie automatically.

Do not manually read the session cookie.

Do not create a recovery cookie.

Do not create a reset-token cookie.

Do not add Authorization headers containing recovery secrets.

==================================================
MOBILE RESPONSIVENESS
==================================================

The recovery flow must work on the existing Teacher Login responsive/mobile layout.

Do not redesign the whole Teacher Login page.

Only make the minimum UI changes necessary for the new recovery stages.

Ensure:

- school code input fits mobile width
- email input fits mobile width
- OTP input fits mobile width
- password fields fit mobile width
- buttons remain usable
- no horizontal overflow
- error messages remain readable

==================================================
NORMAL TEACHER LOGIN
==================================================

Do not change the existing normal login contract.

Verify that:

- email/password login still works
- login errors remain unchanged
- successful login behavior remains unchanged
- recovery flow does not automatically authenticate
- recovery UI can return to normal login

==================================================
IMPORTANT: NO PHONE NUMBER
==================================================

The old Teacher recovery flow used email + phone.

Remove the phone requirement from the frontend recovery flow.

Teacher recovery now uses:

School Code + Registered Teacher Email

Only.

Do not send phone to the backend.

==================================================
IMPORTANT: NO TEACHER ID
==================================================

The frontend must not know or submit Teacher ID.

Do not reintroduce Teacher ID into:

- request body
- hidden fields
- route parameters
- URL query
- React state unless absolutely necessary for display (it should not be necessary)

==================================================
IMPORTANT: NO RESET TOKEN
==================================================

The frontend must never receive or manage the reset token.

If the backend response somehow contains a resetToken unexpectedly:

- do not store it
- do not display it
- do not put it in the URL
- report the unexpected response shape through development-safe error handling

Do not weaken the backend to accommodate it.

==================================================
TESTS
==================================================

Update/add frontend tests for Teacher Forgot Password.

At minimum cover:

1. Normal Teacher Login screen renders.

2. Forgot Password opens recovery stage.

3. Recovery form shows School Code + Email.

4. Phone field is not required.

5. Forgot Password sends exactly:
   {
     schoolCode,
     email
   }

6. Forgot Password does not send:
   - schoolId
   - tenantId
   - teacherId
   - userId
   - challengeId
   - resetToken

7. Generic Forgot Password response moves to OTP stage.

8. OTP stage displays six-digit OTP input.

9. OTP request sends exactly:
   {
     otp
   }

10. OTP request does not send identity fields.

11. Successful OTP verification moves to password-reset stage.

12. Frontend does not expect a resetToken.

13. Frontend does not store resetToken.

14. Password-reset request sends exactly:
   {
     newPassword,
     confirmPassword
   }

15. Password-reset request does not send identity fields or resetToken.

16. Password mismatch is rejected client-side.

17. Password below minimum length is rejected client-side.

18. Valid matching password can be submitted.

19. Successful reset shows success state.

20. Successful reset does not automatically log in.

21. Back to Login clears recovery UI state.

22. Invalid OTP displays safe generic error.

23. Expired recovery session displays safe generic error.

24. Rate-limit response displays safe generic error.

25. Provider errors are never exposed.

26. OTP is never written to localStorage/sessionStorage.

27. resetToken is never written to localStorage/sessionStorage.

28. challengeId/userId/teacherId/schoolId are not stored in browser recovery state.

29. Duplicate Forgot Password submission is prevented.

30. Duplicate OTP submission is prevented.

31. Duplicate password-reset submission is prevented.

32. Mobile layout renders without horizontal overflow.

33. Normal Teacher Login remains functional.

==================================================
IMPORTANT TEST IMPLEMENTATION RULE
==================================================

Do not weaken or mock away the security contract simply to make frontend tests pass.

Mock API responses only where appropriate.

Verify exact request bodies.

Specifically assert that forbidden identity fields are absent.

==================================================
NO BACKEND CHANGES
==================================================

After implementation, check:

git diff

The expected production change for Step 8 should be frontend-focused.

If you discover that backend changes are required because the frontend/backend contracts do not match:

STOP.

Do not silently modify backend security code.

Report the mismatch separately.

==================================================
VALIDATION
==================================================

Run:

1. Teacher Login frontend tests.
2. New Teacher Forgot Password frontend tests.
3. Existing Teacher authentication tests.
4. Existing Teacher recovery/security tests.
5. Existing Admin recovery tests.
6. npm run check
7. git diff --check
8. Restart application.
9. Confirm application starts successfully.
10. Confirm normal Teacher Login page renders.

If browser testing infrastructure is available, manually verify:

Desktop:
- Login
- Forgot Password
- School Code + Email
- OTP
- New Password
- Success
- Back to Login

Mobile-width:
- same flow
- no horizontal overflow

Do NOT perform a real password reset against a production account.

Use the existing development/test account and mocked/test infrastructure where appropriate.

==================================================
STRICT SCOPE
==================================================

Step 8 may modify:

- client/src/pages/teacher-login.tsx
- frontend test files specifically related to Teacher Login/recovery
- frontend-only helper code strictly required by Teacher recovery

Do NOT modify:

- server/teacher-password-recovery-routes.ts
- server/teacher-password-recovery-session.ts
- server/password-recovery.ts
- server/teacher-password-recovery.ts
- server/storage.ts
- server/session-revocation.ts
- server/index.ts
- database schema
- migrations
- legacy Teacher OTP/reset columns
- SendGrid configuration
- SMS configuration
- normal Teacher authentication backend

unless a genuine contract mismatch is discovered; if so, STOP and report it.

==================================================
FINAL REPORT
==================================================

Return:

A. Exact files changed
B. Teacher Login recovery stages implemented
C. Forgot Password request body
D. Verify OTP request body
E. Reset Password request body
F. Confirmation that browser identity fields are never sent
G. Confirmation that resetToken is never received/stored by frontend
H. Confirmation that OTP is never stored in browser storage
I. Password validation behavior
J. Error handling behavior
K. Loading/double-submit protection
L. Mobile responsiveness result
M. Normal Teacher Login regression result
N. Tests added
O. Test results
P. npm run check
Q. git diff --check
R. Application startup
S. Backend files unchanged confirmation
T. Database/schema/migration status
U. Any contract mismatch or security issue discovered

FINAL STATUS:

PASS
or
BLOCKED

Do not proceed to Step 9 automatically.