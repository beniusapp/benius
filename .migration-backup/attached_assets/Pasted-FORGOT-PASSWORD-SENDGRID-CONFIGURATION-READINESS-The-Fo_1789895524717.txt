FORGOT PASSWORD — SENDGRID CONFIGURATION READINESS

The Forgot Password implementation is currently BLOCKED only because the intended development school's SendGrid configuration is missing/disabled.

Do NOT modify the Forgot Password implementation.

Do NOT modify password-reset logic.

Do NOT use another school's SendGrid configuration.

Do NOT change tenant isolation.

Do NOT send any real email yet.

This is ONLY a configuration-readiness investigation.

==================================================
1. INSPECT CURRENT EMAIL CONFIGURATION
==================================================

Inspect the existing email/SendGrid implementation and determine exactly which configuration values are required for the intended development school.

Identify:

- SendGrid API key environment variable name
- verified sender email environment variable/configuration name
- sender name, if required
- school-level email-provider configuration fields
- whether email delivery is enabled by a database/config flag
- any other required non-secret configuration

Do NOT reveal the value of any secret.

Do NOT print API keys.

Do NOT print passwords.

Do NOT print tokens.

Only report the VARIABLE/CONFIGURATION NAMES and whether each required value is:

- configured
- missing
- disabled
- invalid/unverified, if this can be safely determined without exposing secrets

==================================================
2. SCHOOL/TENANT SCOPE
==================================================

Identify exactly which development school is being used for the intended Forgot Password test.

Do not inspect or use another school's credentials.

Confirm that the test account, recovery email, school code, email-provider configuration, and SendGrid configuration all belong to the same development tenant.

Do not modify any school.

==================================================
3. SENDGRID REQUIREMENTS
==================================================

Determine whether the intended development school requires:

- SendGrid API key
- verified sender identity
- email delivery enabled
- recovery email configured

Report the missing requirements.

Do not create or change credentials.

==================================================
4. REPLIT SECRETS
==================================================

Determine whether the required secret is expected to be supplied through:

- Replit Secrets
- environment variables
- database configuration
- another existing secure configuration mechanism

Do not create a new secret-management system.

Do not place secrets in source code.

Do not place secrets in git.

Do not print secret values.

==================================================
5. DATABASE SAFETY
==================================================

Do not:

- run migrations
- modify schema
- modify users
- modify schools
- modify recovery emails
- modify password-reset challenges
- modify production data

This is a read-only investigation.

==================================================
6. PROVIDE SETUP INSTRUCTIONS
==================================================

Return exact instructions for the application owner to configure the intended development school's email delivery.

The instructions must clearly distinguish:

A. Values that must be entered into Replit Secrets/environment configuration

B. Values/settings that must be configured in the school/provider configuration

C. Values/settings that must be verified in SendGrid

Do not ask the application owner to paste secrets into chat.

Do not expose secrets in the report.

==================================================
7. SENDGRID SENDER REQUIREMENT
==================================================

Explain exactly what sender verification is required.

If the sender address must be verified in SendGrid, state that clearly.

Do not attempt to bypass sender verification.

==================================================
8. DO NOT IMPLEMENT A FALLBACK
==================================================

Do NOT:

- use Mailtrap instead
- use another school's credentials
- return OTP through the API
- display OTP in the UI
- log OTP
- create a development backdoor
- disable OTP delivery security
- bypass email delivery

==================================================
9. FINAL REPORT
==================================================

Return:

# SENDGRID CONFIGURATION READINESS REPORT

## Intended Development School
State the school/tenant identifier in a non-sensitive form.

## Required Configuration

| Configuration | Status |
|---|---|
| Email delivery enabled | |
| SendGrid API key configured | |
| Verified sender configured | |
| Recovery email configured | |
| School/provider mapping | |
| Any other required configuration | |

Do not include secret values.

## Required Replit Secrets
List only secret VARIABLE NAMES.

## Required School Configuration
List required non-secret settings.

## Required SendGrid Configuration
List required verification/setup.

## Exact Owner Setup Steps
Give numbered setup instructions.

## Security Check
Confirm:
- no secrets exposed
- no credentials modified
- no other tenant accessed
- no database changes
- no code changes

## FINAL STATUS

Choose exactly one:

READY — configuration can now be supplied and live email test can be rerun

or

BLOCKED — additional application/configuration investigation required

STOP HERE.

Do not modify anything.
Do not send an email.
Do not proceed with the live delivery test.