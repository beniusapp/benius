ROLLBACK DEVELOPMENT APPLICATION TO COMMIT d4b11e6

IMPORTANT — READ THIS ENTIRE INSTRUCTION BEFORE DOING ANYTHING.

I am currently BUILDING/DEVELOPING this School MIS application.

THIS IS NOT A PRODUCTION PHASE APPLICATION.

My intention is to completely return the DEVELOPMENT application to the state represented by this commit:

COMMIT:
d4b11e6f46a5a87c6b87c267edeb5f17aca7a634

COMMIT MESSAGE:
"Add investigation prompt asset"

I want this commit to become the new clean development baseline.

I do NOT want the application changes introduced after this commit.

==================================================
PART 1 — CURRENT STATE SAFETY CHECKPOINT
==================================================

BEFORE performing any rollback:

Create or confirm a current safety checkpoint/snapshot of the CURRENT development state.

The current HEAD is:

2fe59a75dc104161a5f50eda58277ef2a3a4c73a

The safety checkpoint must preserve the current development code and, if supported, the current DEVELOPMENT DATABASE state.

This safety checkpoint is ONLY so that the current development work can be recovered if necessary.

DO NOT modify production.

DO NOT rollback production.

DO NOT modify production database.

DO NOT delete production data.

DO NOT deploy anything to production.

==================================================
PART 2 — VERIFY THE TARGET
==================================================

Verify that:

d4b11e6f46a5a87c6b87c267edeb5f17aca7a634

exists and is the exact rollback target.

Confirm that this is the September 1, 2026 commit:

"Add investigation prompt asset"

Do not substitute another commit.

==================================================
PART 3 — ROLLBACK INTENTION
==================================================

I want the DEVELOPMENT application restored to the state represented by:

d4b11e6

I want all application/code changes introduced after that commit removed/reverted.

This includes post-target changes to:

- academic calculation engine
- academic calculation service
- academic logging
- academic term boundaries
- Teacher Examination changes
- Student Examination changes
- Student Archives changes
- Performance Analytics changes
- Faculty Mapping changes
- promotion changes
- enrollment/session academic changes
- teacher route changes
- storage changes
- schema changes
- academic frontend changes
- post-target tests
- post-target instruction/reference assets
- post-target screenshots
- other tracked application changes

The target state should be the actual code state of d4b11e6.

==================================================
PART 4 — DATABASE ROLLBACK
==================================================

THIS IS A DEVELOPMENT DATABASE.

If the Replit checkpoint rollback interface provides a DATABASE rollback/snapshot corresponding to the target checkpoint, include the DEVELOPMENT DATABASE in the rollback.

The goal is:

TARGET CODE + TARGET DEVELOPMENT DATABASE

rather than:

TARGET CODE + NEWER DEVELOPMENT DATABASE

If a database-inclusive rollback is available and is clearly associated with the target checkpoint, use it.

DO NOT touch production.

DO NOT restore or modify production database state.

==================================================
PART 5 — VERY IMPORTANT DATABASE SAFETY
==================================================

Before executing the database-inclusive rollback, verify that the database being restored is the DEVELOPMENT database.

Do NOT perform destructive SQL manually.

Do NOT manually delete rows.

Do NOT manually drop tables.

Do NOT manually reverse migrations.

Do NOT manually reconstruct historical database state.

Use the Replit checkpoint/database restoration mechanism if it is available.

If the target checkpoint does NOT have a usable database snapshot, DO NOT invent a database rollback procedure.

Instead STOP and report that the target development database snapshot is unavailable.

==================================================
PART 6 — DO NOT PRESERVE POST-TARGET APPLICATION LOGIC
==================================================

Do not selectively merge newer code back into the target.

Do not cherry-pick any commits after d4b11e6.

Do not keep Step 1A.

Do not keep the new Examination roster authorization.

Do not keep the academic calculation engine.

Do not keep the post-target Faculty Mapping implementation.

Do not keep post-target promotion logic.

Do not keep post-target session/enrollment architecture.

Do not keep post-target academic changes.

The purpose of this rollback is to establish d4b11e6 as the clean baseline.

==================================================
PART 7 — DO NOT CHANGE UNRELATED EXTERNAL STATE
==================================================

Do not modify:

- production
- external services
- payment systems
- email services
- external APIs
- production databases
- production deployments

This rollback is for the DEVELOPMENT/BUILDING environment only.

==================================================
PART 8 — EXECUTE ROLLBACK
==================================================

If and ONLY IF:

1. The current development state has been safely checkpointed,
2. d4b11e6 is verified as the target,
3. The rollback target is the correct development checkpoint,
4. A compatible DEVELOPMENT DATABASE snapshot is available,

then perform the Replit checkpoint rollback to d4b11e6.

Use the rollback mechanism appropriate for the Replit environment.

If the rollback UI requires selecting:

CODE / FILES
and
DATABASE

select the options necessary to restore BOTH the target development code and target development database.

Do NOT select anything that affects production.

==================================================
PART 9 — AFTER ROLLBACK
==================================================

After the rollback, DO NOT start implementing new features.

First verify the restored development application.

Check:

1. Git/current commit corresponds to d4b11e6 or the restored checkpoint representing it.
2. The post-target application source changes are gone.
3. The post-target calculation engine is gone.
4. The post-target term-boundary implementation is gone.
5. The post-target Teacher Examination changes are gone.
6. The post-target Faculty Mapping implementation is gone.
7. The post-target promotion/session changes are gone.
8. The development database corresponds to the target snapshot, if a database rollback was performed.
9. Application starts successfully.
10. TypeScript/build validation works.
11. Basic authentication works.
12. No production resources were modified.

Do not perform destructive cleanup after the rollback.

==================================================
PART 10 — IMPORTANT: DO NOT START REBUILDING
==================================================

After rollback:

STOP.

Do not implement STEP 1.

Do not implement STEP 1A.

Do not implement STEP 2A.

Do not implement STEP 2.

Do not modify Examination.

Do not modify Faculty Mapping.

Do not modify Enrollment.

Do not modify Student Registry.

Do not modify Academic Sessions.

Do not modify score saving.

Do not make architectural changes.

We will begin rebuilding from the restored baseline only AFTER I review the rollback result.

==================================================
PART 11 — FINAL REPORT
==================================================

After the rollback, report exactly:

A. SAFETY CHECKPOINT
- Was the current development state checkpointed?
- What checkpoint/identifier was created?

B. TARGET
- Target commit
- Target commit message

C. CODE ROLLBACK
- Was development code restored to d4b11e6?
- Confirm whether post-target tracked code changes were removed.

D. DATABASE ROLLBACK
- Was a development database snapshot available?
- Was it restored?
- What database state was restored?
- Do NOT claim database restoration if it was not actually performed.

E. PRODUCTION SAFETY
- Confirm that production code/database/deployment was NOT modified.

F. VERIFICATION
- Application startup
- TypeScript/build
- Basic tests or checks
- Any errors

G. FINAL DEVELOPMENT STATE
Clearly state whether the development environment is now:

"d4b11e6 code + d4b11e6 development database"

OR, if the database could not be restored:

"d4b11e6 code + existing development database"

Do not hide this distinction.

==================================================
FINAL STOP CONDITION
==================================================

The rollback is the ONLY task.

Do not implement anything after the rollback.

Do not improve anything.

Do not fix anything.

Do not rebuild anything.

STOP and wait for my next instruction.

REMEMBER:

I AM IN THE BUILDING/DEVELOPMENT PHASE.

THIS IS NOT PRODUCTION.

THE GOAL IS TO ESTABLISH d4b11e6 AS THE CLEAN DEVELOPMENT BASELINE.